# Known Bugs & Resolved Issues

This directory documents significant bugs encountered during the port, their symptoms, root causes, and fixes. When fixing a new bug, add an entry here so future sessions can recognize the same class of issue.

## Index

| Date | Slug | Summary |
|------|------|---------|
| 2026-08-25 | [osb5_stale_owner_css_previews](osb5_stale_owner_css_previews_2026-08-25.md) | **FIXED** — With a custom-character bundle attached, vanilla fighters' character-select 3D previews shredded (DK reduced to tie + hands). GObj pool reuse let a despawned owner's address match a newly spawned preview, so the injection's per-tick joint blanking hit a vanilla fighter. Ownership now also requires the fighter kind to match. |
| 2026-08-04 | [native_pointer_segment_e_alias](native_pointer_segment_e_alias_2026-08-04.md) | **FIXED (shared Fast3D interpreter; exposed by Windows OpenGL)** — The fighter-intro sequence crashed deterministically at VI 1805 because Windows ASLR placed the valid native `dFTShadowNoPrevLinkDL` pointer at `0x00007FF60EC0D820`. Segment-E stride correction tested only bits 24–31, mistook the pointer's coincidental `0x0E` byte for an N64 `0x0Exxxxxx` address, and redirected the display-list walker into unrelated heap data. Both segment-E paths now require the complete value to fit in 32 bits before applying N64 address logic. OpenGL widescreen passed 65 seconds (formerly crashed at 30.9); DirectX passed 40 seconds. |
| 2026-08-04 | [fighter_name_opengl_color_mask](fighter_name_opengl_color_mask_2026-08-04.md) | **FIXED (shared Fast3D presentation path)** — OpenGL rendered the opening movie's `MARIO`/DK/YOSHI/SAMUS name sprites correctly into `mGameFb`, but displayed a black card. The final display-list draw redirects color to the Z buffer and left OpenGL's global color-write mask disabled, so the subsequent ImGui framebuffer presentation wrote no pixels; held VI frames repeated the black result. The normal and held-frame presentation paths now restore color writes and synchronize Fast3D's cached state before drawing the game texture. Windows OpenGL and DirectX scene-28 captures both retain the fighter name. |
| 2026-08-04 | [intro_explosion_widescreen_persistence](intro_explosion_widescreen_persistence_2026-08-04.md) | **FIXED (shared Fast3D path)** — The opening desk-to-stage explosion was correct in 4:3 but turned black outside its expanding red mask in widescreen. Normal widescreen setup clears unused 4:3 side strips to prevent stale menu imagery; that destroyed the prior widened room pixels this transition intentionally consumes (and DirectX/Metal's conservative regional-clear fallback cleared the entire framebuffer). A graphics-bridge flag now preserves the complete game framebuffer only while the redirect-to-Z transition is alive, ticks 1040–1079. DirectX captures retain the room to both edges with no black exterior or duplicated corners; OpenGL completed the same scene run cleanly; Metal inherits the shared path but remains untested on Windows. |
| 2026-08-04 | [intro_explosion_d3d_depth_clip](intro_explosion_d3d_depth_clip_2026-08-04.md) | **FIXED (libultraship DirectX 11)** — The opening desk-to-stage red vertex explosion duplicated/smeared mask geometry into the screen corners. Redirect-to-Z mask draws suppress color writes and straddle the camera plane; OpenGL already temporarily disables its global depth clamp for those draws, but DirectX left `DepthClipEnable=false` globally, so the mask rasterized as an inflated unclipped shape and wrote synthesized depth outside the authored region. The DirectX rasterizer cache now keys on the color-write-disabled state and enables real depth clipping only for redirect mask draws, restoring the permissive state for ordinary geometry. Windows Release build passed; user visual gate passed on DirectX and OpenGL in 4:3. |
| 2026-07-31 | [reloc_token_gen_opcode_misparse](reloc_token_gen_opcode_misparse_2026-07-31.md) | **FIXED (port bridge)** — Generation-dependent "time bomb" behind the long-session particle-texture corruption and an `abort()` in `portRelocRegisterPointer` at the 2nd attract cycle. A reloc token's top byte is `gen >> 4`; after ~13 scene transitions hot slots reach gen 16 and mint `0x01xxxxxx` tokens. The chain walk parses each entry's command word from `slot-4` — for adjacent chain entries that's the token it *just wrote* — so at gen 16+ tokens masquerade as G_VTX (gen 4048+: SETTIMG), pass every structural check, and `chain_fixup_vertex` rot16s fresh file bytes (texels → garbled particles; descriptors → walk derails into a token-feedback spiral that exhausts the 20-bit index space → abort). Proven by core autopsy: slot census (524k live, freelist empty, duplicates ×28), frozen walk locals (`next=23` self-loop via token `0x17FFFE`), byte-diff vs the pristine source buffer still alive in the core (rot16×3+BSWAP32 stride-4 fingerprint at an offset no pristine chain entry targets). Fixes: w0-is-live-token guard in `portRelocFixupTextureFromChain` (with corpse pruning); bounds + step-cap containment on all three chain walks (`chainWalk STOP` instead of spiral); `SSB64_RELOC_GEN_SEED` diag env to arm either hazard band at boot (seed 15 = G_VTX, 4047 = SETTIMG). Audit hook: any value space overlapping GBI opcode space will eventually be parsed as a command by a heuristic scanner. |
| 2026-07-30 | [texcache_stale_identity_aliasing](texcache_stale_identity_aliasing_2026-07-30.md) | **FIXED (port + libultraship + decomp PORT blocks)** — Intro corruption family (corrupted particle/stage textures, flat fighters, intermittent SIGSEGV in a stage). Root: `portRelocFixupTextureAtRuntime` trusted any SETTIMG address inside a reloc file and BSWAP32'd "texels" in place — one stale SETTIMG made the renderer byte-reverse live DObjDesc arrays and tokens (proved by paired core autopsy: corrupted core = healthy bytes wholesale BSWAP'd, tokens included). Compounded by `gfxPointerInLoadedModule` counting the non-PIE main image as a "loaded module", structurally disabling the SETTIMG/G_VTX low-address guards on Linux. Root origin: legitimate texture draws whose N64 fixed-size LoadBlock over-reads into adjacent struct data — harmless on hardware, destructive when the fixup mutates source memory. Fixes: chain-slot registry + clamping runtime texfix swaps at the first tokenized slot; main-image exclusion in the module guard; content-hash verification on texture-cache hits (heals address-aliased stale texels, `SSB64_TEXCACHE_VERIFY=0` off); particle-bank lockstep cache eviction; OOB fighter-scale seed clamp; `gEFManagerFiles` scene-boundary scrub. Adds `SSB64_SCENE_HEAP_FREE=1` ASan diagnostic; documents why the arena + token table are never freed by design. |
| 2026-07-31 | [trig_host_libm_divergence](trig_host_libm_divergence_2026-07-31.md) | **RESOLVED (branch agent/cody-waite)** — All game trig (~185 `__sinf`/`__cosf`/bare `sinf`/`cosf` sites) silently used host libm, so physics diverged from console by compounding ulps and differed across Linux/macOS/Windows; N64 quirks (give-up-to-`0.0f` above `2^28`, tiny-arg identity) were missing. The decomp's original SGI Cody-Waite polynomial sources existed but were never compiled — their `du` union `{hi,lo}` double constants assume big-endian MIPS and reassemble as garbage on LE hosts (probably why the host-libm stubs existed). Fixed by compiling `gu/sinf.c`/`cosf.c` with a `DU_INIT` endian macro in `guint.h` (IDO preprocesses byte-identical), `-ffp-contract=off`/`-fno-strict-aliasing`/`-fno-builtin-sinf/cosf`, and a C `__libm_qnan_f`. Harness-verified to 3.6e-8 vs double reference + all N64 edge behaviors. |
| 2026-07-30 | [rcp_sustained_overbudget_stutter](../intro_divergence_triage_2026-07-30.md) | **RESOLVED (issue #136)** — The stutter before the unlockable-character silhouettes: the newcomers scene sustains RCP-model cost 460–515k (white-bg rects + 8 fighters) over the 400k budget on every frame, so the freeze-sim threshold refired N=3 fifteen times in a row (freeze-advance-freeze cadence). Fixed in port_get_last_dl_defer_n: isolated climaxes keep the full authored hold, but sustained over-budget stretches enter a cooldown where the excess accumulates as carry and drops single frames (N=2) at the pace the excess implies (~1 per 4–6 frames there) — matching the accurate emulator's gentle-drop pattern. Authored montage pose freezes (frames 1800/1881) and the natural scene-boundary holds are unchanged. |
| 2026-07-30 | [near_clip_trivial_reject](../intro_divergence_triage_2026-07-30.md) | **RESOLVED (issue #72)** — The "camera blocked by gold wedge" flicker in the Yoshi montage segment: Fast3D's inherited upstream code comments out the CLIP_NEAR trivial-reject flag, so the catch-flash quad — authored entirely outside the near plane (two verts behind the camera, w<0) and always trivially rejected by the RSP — reached the rasterizer on 2 frames per run whenever the garbage clip flags of its negative-w vertices happened not to share a bit, painting a homogeneous wrap-around smear across the motion window. Fixed by restoring CLIP_NEAR as the homogeneous half-space test `z < -w` (valid for both signs of w; all-verts-shared rejection only, so partial near-crossings render as before). Verified against cxd4-LLE RDRAM dumps (hardware never renders the quad) and an 18-checkpoint intro regression sweep. Full evidence chain in the triage doc. |
| 2026-07-30 | [zcmp_depth_test_gating](zcmp_depth_test_gating_2026-07-30.md) | **RESOLVED** — The intro explosion's big red starburst border was missing: Fast3D enabled depth testing from geometry-mode `G_ZBUFFER` alone, but the RDP only compares when `Z_CMP` is set in the render mode. The 128-tri Outline ring draws with `G_ZBUFFER` set and `Z_CMP`/`Z_UPD` clear right after the redirect fill stamps the whole Z buffer to near, so every ring fragment was Z-rejected (hardware paints it unconditionally; the later Z_CMP wallpaper/scene draws cover its interior, leaving the red spikes outside the star). Fixed by gating `depth_test` on `Z_CMP && G_ZBUFFER` for all draws. Verified: red coverage now ramps to hardware levels across the transition; no regressions across intro, title, and demo fight. |
| 2026-07-30 | [color_image_redirect_z_emulation](color_image_redirect_z_emulation_2026-07-30.md) | **RESOLVED (issue #10)** — Real emulation of the N64 color-image-redirect-to-Z idiom in Fast3D: redirect-active fills become value- and region-aware depth clears, redirect-active tris write their constant combiner output as depth (via the G_ZS_PRIM path) with framebuffer color writes suppressed, rectangles depth-test/write with prim depth when Z_CMP/Z_UPD are set (RDP semantics), and the game-FBO color persists across frames (widescreen now clears only the pillarbox strips). Hardware ground truth established by dumping RDRAM Z-buffer contents from a cycle-accurate mupen64plus+angrylion reference mid-transition. The two `#ifdef PORT` workarounds in `mvopeningroom.c` (Overlay skip, camera priority 30) are removed; the desk→stage explosion now peels the frozen prior-frame room away to the destination like hardware. See also `docs/intro_divergence_triage_2026-07-30.md` for the #72/#136 findings from the same session. |
| 2026-07-12 | [reset_command_unregistered](reset_command_unregistered_2026-07-12.md) | **FIXED (port + decomp PORT blocks)** — The ESC-menu Reset button and Ctrl/Cmd-R did nothing: both `Dispatch("reset")` at the LUS console but no `reset` command was ever registered. Fixed in three layers: the port registers `reset` → `portSCManagerRequestReset()` (scmanager.c) requests an in-game return to the boot scene via the normal scene-exit path; the reset is STICKY because `sc1PManagerUpdateScene` chains 1P sub-scenes and rewrites `scene_curr` itself (a mid-1P-match reset crashed in `ftManagerSetupFilesAllKind` before `SC1P_PORT_RESET_BAIL()` guards were added after all 12 sub-scene calls); and it sets the game's native `is_reset` abort flag (the A+B+R+Z combo's) so battle-end interpreters cooperate — without it a VS reset chained into Sudden Death. Grep-audit: `scmanager.c` + `sc1pmanager.c` are the only scene-chaining files. |
| 2026-07-10 | [android_v7a_minsdk_too_high](android_v7a_minsdk_too_high_2026-07-10.md) | **FIXED (working tree)** - The `armeabi-v7a` v1.5 APK failed to install on pre-Android-8.0 devices with `INSTALL_FAILED_OLDER_SDK` because `minSdk` defaulted to 26 — a copy-pasted skeleton value, never an analyzed floor. The only real API-26 dependency was a *forced* AAudio audio-driver hint (now gated at runtime via `android_get_device_api_level()`, falling back to OpenSL ES below 26). The true 32-bit-ARM floor is API 24 (libc++ `<fstream>` needs `fseeko`/`ftello`, `__INTRODUCED_IN(24)` on ILP32). `minSdk` lowered 26 → 24 (Android 8.0 → 7.0). Reproduced + verified on a real ARMv7 API-25 emulator. |
| 2026-07-06 | [android_v7a_release_artifact](android_v7a_release_artifact_2026-07-06.md) | **FIXED (release packaging)** - The `armeabi-v7a` Android build existed only as a standalone debug validation workflow, so tag releases uploaded only the default `arm64-v8a` `BattleShip-android.apk`. Release CI now builds Android through an ABI matrix, keeps the historical arm64 filename, uploads `BattleShip-android-armeabi-v7a.apk` for 32-bit devices, and publishes both APKs in the draft release. |
| 2026-07-06 | [macos_sdl2compat_sdl3_dlopen](macos_sdl2compat_sdl3_dlopen_2026-07-06.md) | **FIXED (release packaging)** - macOS v1.5 could launch to "Failed loading SDL3 library" because Homebrew's current `sdl2` can resolve to `sdl2-compat`, which opens SDL3 via `dlopen()` rather than a Mach-O load command. `dylibbundler` bundled `libSDL2-2.0.0.dylib` but missed `libSDL3.dylib`. The package script now detects sdl2-compat markers, copies/signs `libSDL3.0.dylib` into `Contents/Frameworks`, and creates the `libSDL3.dylib` symlink it probes. |
| 2026-07-06 | [macos_dmg_styled_release](macos_dmg_styled_release_2026-07-06.md) | **FIXED (release packaging)** - v1.4+ macOS release DMGs lost the styled `create-dmg` Finder window because `025c8b86` made CI prefer a plain `hdiutil` image after GitHub's macOS runner image hung in Finder/AppleScript styling for 30+ minutes. Release CI now explicitly sets `DMG_STYLED=1`, `DMG_REQUIRE_STYLED=1`, and `DMG_TIMEOUT=300`, so the polished banner/large-icon drag-to-Applications installer is required while still timing out instead of hanging indefinitely. |
| 2026-07-06 | [tinycc_av_flagging](tinycc_av_flagging_2026-07-06.md) | **FIXED LOCALLY (default Windows package)** - The Windows package shipped `tcc.dll` and compiled source mods to temp DLLs before loading them, matching TinyCC binary signatures and DLL-drop/load heuristics. `libtcc` is now static-linked, source mods relocate through TinyCC memory output, `.tcc` ships only headers + `libtcc1.a` + `BattleShip.def`, and the package script fails if `.tcc` contains `.dll`/`.exe`. Latest public `BattleShip-windows.zip` reproduced Defender `Trojan:Win32/Suschil!rfn`; the new default `BattleShip-windows.zip` passed static, MotW, runtime-hook, and hot-reload Defender checks. VirusTotal and WDSI submission remain external/account-backed follow-ups. |
| 2026-06-29 | [android_landscape_lock_sdl_fulluser](android_landscape_lock_sdl_fulluser_2026-06-29.md) | **FIXED (`agent/android-orientation-touch`)** — The branch's "lock to landscape" change left the game rendering portrait on a portrait-held device (overlay squished). The manifest's `screenOrientation="landscape"` and an `onCreate` `setRequestedOrientation` call are both overridden at runtime: `BattleShipActivity` extends SDL's `SDLActivity`, whose `setOrientationBis()` requests `SCREEN_ORIENTATION_FULL_USER` for our resizable window (empty `SDL_HINT_ORIENTATIONS`), letting device rotation win. Fixed by overriding `setRequestedOrientation()` to coerce every request to `SENSOR_LANDSCAPE`. Same review also hardened the fixed-anchor touch stick's left-half region to re-derive its width on every layout (one-shot `post()` went stale across `configChanges` resizes). Audit hook: manifest `screenOrientation` does not hold for a resizable `SDLActivity`; verify orientation on a portrait device. |
| 2026-06-24 | [css_p34_reactivate_null_fkind](css_p34_reactivate_null_fkind_2026-06-24.md) | **FIXED (issue #244)** - Android four-virtual-controller CSS re-entry exposed connected P3/P4 `NA` slots after a two-player match. Activating one cycled it back to HMN and called `mnPlayersVSUpdateFighter` with `fkind == nFTKindNull` and no fighter GObj; the old skip logic only ran when a fighter GObj already existed, so the null fighter kind reached costume/fighter setup tables and crashed. PORT fix skips empty/unselected slots even when no fighter GObj exists yet, while preserving the original non-PORT control flow. |
| 2026-06-24 | [android_extra_stage_css_icons](android_extra_stage_css_icons_2026-06-24.md) | **FIXED (wiring 2026-06-24; verified on-device 2026-06-29)** — Android showed question-mark/blank icons for Final Destination, Metal Cavern, and Battlefield because the runtime CSS asset loader expects `externalFilesDir/assets/css_icons/<stage>_small.png`, but Android only generated `BattleShip.o2r` from the user's picked ROM and never ran the desktop PNG derivation step. `libtorch_runner.so` now derives the extra-stage background and small-icon PNGs from the staged user ROM immediately after O2R extraction, keeping release APKs free of ROM-derived assets. **Follow-up:** first on-device emulator run found the derivation produced `0/3` icons — `kRelocDataStart` was hardcoded `0x1AEAA0` instead of `0x1B2C6C` (`tableRomAddr + (fileCount+1)*entrySize`, per torch `GetRelocLayout`), so every VPK0-compressed stage file decoded from the wrong ROM offset. Now derived from the file count; emulator run reports `3/3` and the six PNGs are pixel-identical to the desktop oracle. |
| 2026-06-24 | [training_mode_extra_stage_scroll](training_mode_extra_stage_scroll_2026-06-24.md) | **FIXED (issue #243)** — Training Mode could not R-scroll from the original stage page to Final Destination / Metal Cavern / Battlefield because `mnMapsCheckLocked` hid those port-added stages whenever `sMNMapsIsTrainingMode` was true. The page-jump landing scan reused that lock check, saw page 1 as empty, and fell back to in-page movement. Removed the Training-only lockout while keeping empty slots locked. |
| 2026-06-16 | [overscan_black_border](overscan_black_border_2026-06-16.md) | **FIXED (libultraship + port menu)** — A black border framed the game image on all four sides in every window size and fullscreen (distinct from the 4:3 pillarbox; predates the libultraship integration). Root cause: SSB64 bakes a 10px "title-safe" overscan margin into its own GBI — the default scissor (`sys/rdp.c` `syRdpResetSettings` → `(10,10)..(310,230)`) and every per-camera scissor (`sys/objdisplay.c` `dGCCameraScissor{...}=10`) clip all drawing *and the per-frame framebuffer clear* to the safe area, so the 10px margin is never drawn into or even cleared. A CRT's overscan hid it; presenting the full 320×240 framebuffer 1:1 to a desktop window exposes it. Can't just widen the scissor (would reveal uncleared garbage in the margin). Fix: present-level overscan crop in `Gui::DrawGame` — the `ImGui::Image` blit samples the framebuffer minus its proportional margin (`10/320` x, `10/240` y), stretching the safe area to fill the viewport, reproducing the on-TV image. Resolution-independent, orientation-symmetric, skipped in pixel-perfect mode. CVar-gated (`gAdvancedResolution.Overscan.{Enabled,Horizontal,Vertical}`, default on/10/10) with menu toggle + sliders under Enhancements; takes effect immediately (no reload). Audit hook: "image inset from window" → distinguish 4:3 pillarbox (left/right only) vs this overscan frame (all four sides) vs an ImGui dockspace sizing bug (would also affect menu bar / floating windows). |
| 2026-06-15 | [funchook_macos_arm64_patch_anchor](funchook_macos_arm64_patch_anchor_2026-06-15.md) | **FIXED (build)** — TCC mod funchook inline detours silently failed on Apple Silicon (`mprotect(RWX)` → EINVAL on code-signed `__TEXT`) even with JIT entitlements + maxprot re-sign. The FetchContent patch that rewrites funchook to `mach_vm_protect`/`VM_PROT_COPY` never reached the call sites: its CMake anchors used `\;`, which renders as a literal backslash-semicolon, so `string(REPLACE)` no-op'd and only the (now-dead) helper defs got inserted. Fixed anchors to plain `;` + hardened the idempotency guard to key on the wrapped call site (not the helper name). Scripting/events/symbol-resolution were unaffected; only raw detours. Wipe `build*/_deps/funchook-*` to re-patch. Verified via `workspace/hooktest` (1263 detour frames). |
| 2026-06-10 | [controller_port_routing](controller_port_routing_2026-06-10.md) | **FIXED (libultraship)** — Second gamepad (any type) effectively unusable for local multiplayer: three independent defects. (1) Input-editor device rows derived ImGui IDs from device names → two same-model pads collided (warning + hover cross-talk); fixed with instance-id ID suffix + `#1`/`#2` display ordinals. (2) `RefreshConnectedSDLGamepads` ran on every device add/remove event and re-ignored every pad for ports 2-4, silently reverting user routing; now only never-seen instance ids get defaulted. (3) All pads defaulted to port 1 AND only port 1 got default mappings, so a pad routed to port 2 was dead without a manual "Set Defaults"; now Nth pad auto-assigns to first free port and ports 2-4 seed SDL defaults at init. Diagnostic gotchas: same-model pads share SDL GUIDs (per-model, not per-unit); serials are null under macOS GCController driver; headless SDL probes see zero pads without a pumped runloop |
| 2026-06-10 | [crash_handler_devnull_probe](crash_handler_devnull_probe_2026-06-10.md) | **FIXED — corrects a 2026-06-09 audit item** — Crash handler double-faulted on any game-coroutine crash: `ProbeReadable` wrote probed bytes to /dev/null, whose driver (XNU `nullwrite` / Linux `write_null`) never copies from the source buffer, so the probe reported EVERY address readable. `WalkFPChain` then stepped onto the adjacent coroutine stack's `PROT_NONE` guard page at the chain's base frame → nested SIGBUS inside the handler, original backtrace lost, macOS report blamed `port_watchdog.cpp`. Fingerprint: two `!!!! CRASH` lines in ssb64.log, second always SIGBUS at a page-aligned addr. Fix: probe via self-pipe (pipe writes copyin → genuine EFAULT), fds created at watchdog init, drained after each probe. Audit hook: a `write()`-based readability probe must target a pipe/socket, never /dev/null |
| 2026-06-09 | [stale_o2r_recipe_mismatch](stale_o2r_recipe_mismatch_2026-06-09.md) | **FIXED (issues #217/#221/#225)** — Binary updates kept loading a BattleShip.o2r extracted by an older torch/yaml pipeline; format skew read as reloc-token storms (raw floats/host-pointer halves rejected as tokens in the sprite-decode loop) and ESC-menu crashes until users manually wiped and re-extracted. CMake now bakes an asset-recipe hash (torch SHA + config.yml + yamls); extraction stamps a `<o2r>.recipe` sidecar; boot auto re-extracts on mismatch with non-blocking fallbacks (old archive untouched until a replacement fully builds) |
| 2026-06-09 | [fd_vs_music_silent](fd_vs_music_silent_2026-06-09.md) | **FIXED (issue #228)** — VS on Final Destination went silent after the Master Hand sting: FD's map bgm_id is the one-shot entrance sting (1P final battle depends on it and switches to the real theme only after the entrance cutscene), and VS's generic mpCollisionSetPlayBGM has no follow-up. PORT fixup in scvsbattle re-aims at nSYAudioBGMLast after both battle-start and sudden-death BGM calls |
| 2026-06-09 | [stability_audit_sweep](stability_audit_sweep_2026-06-09.md) | **FIXED (multi-subsystem sweep)** — Seven parallel platform-wide audits; ~25 fixes landed. Headliners: Metal `DrawTriangles` vertex-pool `memcpy` had no bounds check (heap overflow on draw-heavy frames, macOS) + `MTLDepthStencilState` leaked per depth-mode change; all 12 `ImportTexture*` replacement paths dereferenced `mMaskedTextures.find()->second` unguarded (UB on miss); WASAPI `IMMNotificationClient` never unregistered (dangling-`this` callbacks on Windows device changes); `TextureCacheKey` hash ignored 8 of 9 compared fields (bump-heap address reuse degraded the texture cache to linear scans); coroutine stacks got mmap `PROT_NONE` guard pages on POSIX/Android (silent heap corruption → clean SIGSEGV); Android touch-overlay virtual-joystick statics survived SDL_Quit across Activity relaunch (UAF on first touch) + Torch `Companion` leaked the full 64 MB ROM into the game heap + `density` missing from `configChanges`; crash-handler FP walk now probes readability via write-to-/dev/null before dereferencing (nested-fault → silent SIGKILL). Decomp sweep across all 7 known bug classes: zero new instances. Notable false positives documented: audio "thread" races (it's a coroutine on the game thread), HiResPack lookup races (single-threaded interpreter), silence-buffer OOB (clamp already present) |
| 2026-06-01 | [android_ultrawide_letterbox](android_ultrawide_letterbox_2026-06-01.md) | **FIXED (issue #209)** — Android 20:9 devices could compatibility-letterbox / relaunch into an apparent 4:3 aspect. Renderer-side widescreen was already dynamic via LUS `mGameWindowViewport`; failure was Android presentation policy. Manifest now opts the app and both activities into resizeable wide-aspect presentation (`maxAspectRatio=2.4`), and Android boot forces `gSdlWindowedFullscreen=1` so LUS uses `SDL_WINDOW_FULLSCREEN_DESKTOP` instead of exclusive fullscreen modes. |
| 2026-05-23 | [grab_pose_eulerextract_gimbal](grab_pose_eulerextract_gimbal_2026-05-23.md) | **RESOLVED — the actual fix for the reported "victim upside-down in grab hold" bug, superseding the same-day stale-transform-cache diagnosis below.** Pikachu→Samus on Dreamland the cleanest repro (DK NOT a reliable canary despite TC handoff doc claim); Windows/Linux primarily. `ftCommonCapturePulledRotateScale` reads the grabber's heavy-item hand world matrix and runs `func_ovl2_800EDA0C` (`gm/gmcollision.c:285`) to extract Euler angles for the victim's rotate. Function handles gimbal lock (yaw ±90°, roll undefined) with `if ((dst[0][2] == -1.0F) \|\| (dst[0][2] == 1.0F))` — **exact float equality**. Pikachu grab matrix composes to `dst[0][2] = 0.9999923110` on clang/x86-64+arm64 (vs. probably-exactly-1.0 on IDO/MIPS), exact-equality fails, general extraction branch runs, `atan2(0.0030, 0.0025)` extracts ~50° garbage roll from quantization noise → victim renders tilted ~50° forward + sideways (reads as "upside-down with torso in floor" for tall victims; squat characters mask it). Fix `#ifdef PORT`: replace exact equality with `<= -0.9999F \|\| >= 0.9999F` (≈ within 0.81° of pure ±90° yaw, narrow enough that genuine rotations don't trip it). One-line change in `gm/gmcollision.c`. Earlier-same-day stale-cache fix (`grab_pose_stale_root_transform`) was a no-op for this — runtime trace (`SSB64_GRAB_TRACE=1`) showed `root_cache_pre=0, hand_cache_pre=0` on every one of 975 traced frames — kept as belt-and-suspenders for save-state / rollback restore paths. Audit hook: ANY matched-decomp comparison of a chain-multiplied float against an exact constant (`== 1.0F`, `== 0.0F`, `== ±k`) is a port-stability landmine; sibling family with `audio_envmix_ramp_overflow_2026-05-08` |
| 2026-05-23 | [grab_pose_stale_root_transform](grab_pose_stale_root_transform_2026-05-23.md) | **SUPERSEDED — reverted same day after JR confirmed the bug still reproduces on `main` with this fix applied AND that tolerance-only (the gimbal-lock fix above, on its own) corrects the visual.** Runtime trace showed the invalidate had nothing to clear (cache flags = 0 on every frame). The full machinery — `ftParamInvalidateFighterRootChain` + `ftMainApplySlopeContourFlags` + per-frame stale-FULL guard + slope-proc invalidate + capture/throw NULL guards — reverted in `398f02978`. Bug summary preserved below for diagnostic-trail value (it's the wrong hypothesis a careful reader landed on from the TC fork's handoff doc, and shows how the data ultimately falsified it):** — During grab hold (Ness → DK most visible; affected all grabber/victim pairs), victim could appear upside-down with feet on the grabber's floor and torso clipped into the ground. Multiple Windows/Linux reports; not reliably reproducible locally. Root cause: `ftCommonCapturePulledRotateScale` derives victim X/Z/rotation from the grabber's heavy-item hand-joint world matrix via `func_ovl2_800EDBA4(hand)`, which lazily walks `dobj->parent` up looking for a fresh `FTParts.unk_dobjtrans_0x5` cache. Each frame `ftParamsUpdateFighterPartsTransformAll(TopN)` clears caches on TopN and descendants but **not the root DObj above TopN**, so a stale root cache from a previous frame short-circuits the walk-up at the root and the hand matrix reflects the prior body pose; Y is independently floor-snapped → "feet on ground, torso in floor." Two secondary leaks: `mpCommonUpdateFighterSlopeContour` wrote a fresh root `rotate.x` from floor angle without invalidating caches; scripts that cleared `FTSLOPECONTOUR_FLAG_FULL` without zeroing the root pitch left stale tilt bleeding into unrelated transforms. Fix (PORT-only): `ftParamInvalidateFighterRootChain(GObj*)` walks TopN→root clearing the cache word, called before every `func_ovl2_800EDBA4(grabber_hand)` read, after `mpCommonUpdateFighterSlopeContour` writes pitch, and inside a new `ftMainApplySlopeContourFlags` helper that pairs every FULL-flag clear with a pitch-zero + invalidate (motion-event `SetSlopeContour`, `ftMainSetStatus` slope clear, per-frame guard in `ftMainProcPhysicsMap`). Plus defensive NULL guards on `capture_gobj`/`capture_fp`/`throw_desc` in capture/throw paths (rollback-resim safety). Ported the engine-only subset of TC fork's `f71ef4b6c` + `f8ae7c9b5` + `c47d17625`, with helper renamed (original `…FromRoot` read the wrong direction), redundant byte writes dropped (already covered by the union word), misattributed ROM-address comment removed, and TC's `mpCommonVec3fPointerUsable` magic-number pointer filter excluded. Audit hook: any `func_ovl2_800EDBA4(joint)` call where the joint sits below TopN needs a root-chain invalidate first if the root rotate/translate was just modified |
| 2026-05-23 | [zebes_acid_effect_stale_dl_link_holder](zebes_acid_effect_stale_dl_link_holder_2026-05-23.md) | **FIXED 2026-05-23.** Sibling of the `dl_link_stale_pointer_guard_2026-05-09` family that the prior write-up couldn't identify the holder for. Linux-only intermittent (~40% per run, 100% per build) particle + stage-geometry corruption in attract mode + persisting into matches: dropped/garbled particles (dust, gun flash, DK floor), corrupted stage backgrounds, Mario's intro background. Augmented `gcDrawDObjTreeDLLinks` bail log with GObj context + 16-byte stale-memory dump, added a per-allocation tracking macro on the four `gcMakeGObj*` wrappers in `objman.c` filtered to `id=1010` (Ground) / `id=1011` (Effect), cross-referenced bailed gobj address against alloc records, addr2line'd the caller LR → pinned to **`grZebesMakeAcid` (Ground GObj at `gGRCommonStruct.zebes.map_gobj`, never cleared)** and **`efManagerMakeEffect` (Effect GObj)**. The decomp has no per-stage Destroy hook and no generic mass-clear of `gGCCommonLinks[]`; on N64 libultra task management clears the GObj pool, on PC the pool persists across transitions so leaked GObjs survive forever in the draw chain with `dl_link` pointing into recycled scene-arena memory that the new scene has overwritten with fresh `Gfx[]` data — bail fires every frame, geometry under the stale GObj's subtree doesn't draw. First fix attempt (sweep in `syTaskmanStartTask`) failed: that hook is at **task** boundary while attract cycles many scenes per task. **Landed fix:** at the top of `scManagerRunLoop`'s `while (TRUE)` scene-dispatch loop (alongside the existing Issue #103 `fighter_gobj` NULLing), walk every `gGCCommonLinks[link]` and `gcEjectGObj` each head (with non-progress + 4096-iter runaway clamps), then `memset(&gGRCommonStruct, 0, sizeof(gGRCommonStruct))` to null the stage-specific cached-GObj union slots. Bail count: 28→0 across multiple repro runs. Diagnostic instrumentation in `objdisplay.c` + `objman.c` retained as a tripwire for future regressions. Audit hook: any "Linux-only intermittent visual corruption surviving scene transitions" → first grep `stale dl_link bail` in `ssb64.log`; cross-ref bailed `gobj=` against `gobj_alloc` records; addr2line caller LR pins the leaker |
| 2026-05-22 | [wayland_set_window_icon_renderer_corruption](wayland_set_window_icon_renderer_corruption_2026-05-22.md) | **RESOLVED (Wayland gate; libultraship root fix deferred)** — Native local builds on Fedora 43 (SDL2 2.32, Wayland session) rendered corrupt geometry in attract-mode fighter intros ("Mario's legs gone"), 100% reproducible per build, compiler-independent. Bisected to `9938319 icon(linux): SDL_SetWindowIcon at boot from embedded PNG`. SDL2's `Wayland_SetWindowIcon` uses the `xdg_toplevel_icon_v1` protocol — its SHM-buffer alloc + icon-set requests flush queued compositor traffic, dispatching a delayed `xdg_toplevel.configure` / `wl_output.scale` mid-init that libultraship's Fast3D had already cached past. The cached viewport/scale then drives GBI translation against the wrong dimensions for the rest of the session. A/B verified: disable the call, force `SDL_VIDEODRIVER=x11`, or build inside an ubuntu:22.04 container (SDL2 2.0.20, pre-protocol) all render clean; `SDL_PumpEvents` after the call doesn't help (the bad state is locked in at init, not in the event queue). CI didn't catch it because `ubuntu-22.04` runners are headless and pre-`xdg_toplevel_icon_v1`. Port-side gate: `SetWindowIcon` queries `SDL_GetCurrentVideoDriver()` and returns early on Wayland — installed AppImages still show the icon via the `.desktop` file's `Icon=` (resolved by the WM through `xdg_toplevel.app_id` / `WM_CLASS`); only dev-build raw runs under Wayland fall back to a generic placeholder. Audit hook: any other `SDL_*` call placed between `InitWindow` and the first frame on Wayland is a latent re-trigger; the proper fix is per-frame surface-state re-read in libultraship's Wayland backend |
| 2026-05-18 | [jp_attract_music_settings_update_race](jp_attract_music_settings_update_race_2026-05-18.md) | JP attract/intro BGM silent: `scManagerRunLoop`'s `#ifdef PORT` branch skipped the N64 audio settings-update spin-wait (stale "audio stubbed" comment), so JP (first scene `nSCKindOpeningRoom`, no ~3 s `nSCKindStartup`) calls `syAudioPlayBGM` mid-restart → `syAudioStopBGMAll()` wipes it. Fix: restore the waits with `port_coroutine_yield()` + hang-proof cap (verified: flag clears in 16 yields, full attract, no hang) |
| 2026-05-18 | [jp_submotion_figatree_misclassified](jp_submotion_figatree_misclassified_2026-05-18.md) | JP CSS/opening fighters T-pose (Mario/DK/Fox; battle fine): `generate_yamls.py` keyed its name tables by US ordinals, so JP file_ids 332–356 fell to `reloc_misc/file_0NNN`; `portRelocIsFighterFigatreeFile` classifies by `reloc_submotions/FT` path prefix → halfswap fixup + AObj range skipped → streams stay BE-halfswapped → T-pose. Fix: rebase name tables through the header's `ll_<N>_FileID` map (identity for US ⇒ byte-identical). NOT cosmetic (corrects earlier triage) |
| 2026-05-18 | [jp_us_only_funcs_called_unconditionally](jp_us_only_funcs_called_unconditionally_2026-05-18.md) | JP link failed: `ftParamGetGroundHazardKnockback` / `func_ovl0_800C9F70` are `#if REGION_US`-only (no JP `#else`, same upstream) yet referenced unconditionally; relaxed to `\|\| defined(PORT)` (byte-matching guard on region-agnostic behaviour). Completed the JP binary link |
| 2026-05-18 | [jp_torch_relocfactory_us_hardcoded_table](jp_torch_relocfactory_us_hardcoded_table_2026-05-18.md) | JP asset extraction aborted ("ROM too small … file 474"): Torch's SSB64:RELOC factory hardcoded the US reloc-table address/count; now resolves a per-ROM `RelocLayout` from the header country byte (US `0x1AC870`/2132, JP `0x1ACAF0`/2107). torch submodule (`ssb64`) |
| 2026-05-18 | [jp_gmcolscripts_pikachu_specialhi_unguarded](jp_gmcolscripts_pikachu_specialhi_unguarded_2026-05-18.md) | JP build enablement: port-only `sGMColScriptLinks[]` pointer-fix table referenced the US-only `dGMColScriptsFighterPikachuSpecialHi` script unconditionally; guarded with `#if REGION_US` mirroring the decomp's own table. Only compile fallout from first-ever `REGION_JP` build |
| 2026-05-13 | [updater_appimage_ld_library_path](updater_appimage_ld_library_path_2026-05-13.md) | **RESOLVED (issue #174)** — Linux AppImage updater spawned host `curl` while inheriting AppRun's `LD_LIBRARY_PATH`, so Fedora 44's system curl loaded the AppImage's older bundled `libcrypto.so.3` against the distro `libssl.so.3` and failed before connecting. Updater now strips `LD_LIBRARY_PATH` / `LD_PRELOAD` for Linux child tools, queries `releases/latest` instead of raw tags, uses the release asset URL for the current platform, makes curl fail on HTTP errors, and shows update-check failures instead of reporting "Up to date." |
| 2026-05-11 | [fb_passthrough_v_flip_linux_nvidia](fb_passthrough_v_flip_linux_nvidia_2026-05-11.md) | **RESOLVED on Linux/NVIDIA OGL — V-flip removed** — Issue #157's `FbNeedsSampleVFlip` returning `invertY` was correct for some OGL driver/OS combo (likely D3D11 was always the test surface and OGL was inferred) but inverted from reality on Linux/NVIDIA proprietary 595.x. Sampling an `invertY=true` FBO as `GL_TEXTURE_2D` on this driver doesn't reverse V vs. the rendered storage — `V=0` already reads "game top". With the original flip applied, 1P frozen wallpaper rendered upside-down (stripe-top showed photo-bottom) while VS results photo wipe rendered right-side-up by accident (its bottom-up mesh authoring cancelled with the V-flip). Drop `FbNeedsSampleVFlip` to return false unconditionally. D3D11/Metal already used the same default; macOS-OGL and Windows-OGL untested but if their behavior is opposite of Linux/NVIDIA the fix is to add a runtime / vendor detection inside the method. Verified: Linux/Fedora 43 + NVIDIA 595.58.03 now renders both 1P stage-clear wallpaper and VS-results photo wipe right-side-up; the shipped v0.8.1 AppImage with V-flip enabled exhibits the same upside-down 1P wallpaper on the same machine, confirming the regression isn't local. Audit-hook table in the per-bug file: 1P + VS render direction together pin the V-flip direction exactly. |
| 2026-05-11 | [linux_stale_scene_data_family](linux_stale_scene_data_family_2026-05-11.md) | **PARTIAL — defensive guard for one variant; three others observed unfixed** — Four distinct Linux-only intermittent SIGSEGVs from the same scene-boundary stale-data family (sibling of `dl_link_stale_pointer_guard_2026-05-09`): (1) Kirby Cutter Draw via stale `*file_head` in `efManagerMakeEffect` → `gcSetupCustomDObjs`; (2) mnCharacters fighter init via stale `joints[i]` in `ftMainSetStatus` joint-init loop; (3) `objanim.c:2869` MObjSub token rejection (caught by existing NULL-check, no crash but indicates upstream staleness); (4) libultraship `Fast::gfx_step` opcode fetch from unresolved N64 segment-E pointer `cmd=0xe000000`. All non-deterministic, all Linux glibc + NVIDIA OpenGL; same memory addresses on Win/Mac happen to re-resolve to compatible bytes. Ship-time fix: defensive `*effect_desc->file_head == NULL` guard in `efManagerMakeEffect` — doesn't fire for the observed crash (pointer was non-NULL stale) but cheap safety net for the "file unloaded → global NULLed" cleaner variant. Real fix needs leaf-by-leaf scene-boundary cleanup audits or a structural generation-stamp on every long-lived reference (same shape as `RelocPointerTable`). Audit hook: Linux-only crash reading N64-segment-pattern bits (`0x80808000…`, `0x0e000000…`) or token-impossible gen/idx → walk back to the holder's lifetime, NULL-clear at scene-boundary teardown. |
| 2026-05-09 | [dl_link_stale_pointer_guard](dl_link_stale_pointer_guard_2026-05-09.md) | **RESOLVED (Linux/OpenGL) — defensive** — Linux intro-skip + autonomous Samus pose SIGSEGVed in `gcDrawDObjTreeDLLinks+0xba` (`cmp 0x14b69d0(,%rcx,8),%rax`: OOB index of `sGCForwardDLs[]` via stale `dl_link->list_id`). Underlying cause is a latent Issue #128 family BSS leak — a long-lived holder retains a `DObjDLLink*` into the prior scene's heap; exposed by PR #144's `free(sPrevHeap)` (Linux glibc `madvise(MADV_DONTNEED)`'s freed pages so dangling reads fault, vs. silently return junk on macOS Magazine / Windows LFH). libultraship `beb5b9a1`'s over-strict `< 4 GB` SETTIMG guard layered on top, rejecting valid non-PIE Linux brk-arena pointers in the `0x10000000`–`0x80000000` range. Three-part fix: (A) recycle the 16 MiB scene heap — one alloc, `memset(0)` + port-side cache eviction between scenes — so stale reads return zero instead of SIGSEGV; (B) PORT-gated bound-check + iteration cap in `gcDrawDObj{Tree,}DLLinks` — bail when `(u32)dl_link->list_id > ARRAY_COUNT(gSYTaskmanDLHeads)` or after walking 64 entries, with throttled `port_log` for future triage; (C) revert libultraship `beb5b9a1`, restoring the original 256 MB N64-segmented-range guard at the bottom of the handler. The specific BSS holder isn't identified; defensive guard catches the symptom universally and the band-aid masks the dangling deref. Verified: clean autonomous attract on Linux/OpenGL through Master Hand + per-character cutscenes including OpeningSamus. |
| 2026-05-09 | [vs_transition_photo_y_flip](vs_transition_photo_y_flip_2026-05-09.md) | **RESOLVED on D3D11/Metal/OpenGL (issue #157)** — VS-match → results-screen photo wipe rendered upside-down on D3D11 release v0.8-beta. The original N64 photo memcpy in `lbtransition.c:326-338` walks the framebuffer **bottom-up** (`framebuffer_pixels -= 310` per row, starts at row 230), so heap row 0 = N64 row 230. The transition mesh's relocData-baked UVs are authored for that bottom-up heap. Our pre-fix `port_capture_register_fb_for_subrect` mapped consumer's local UV (0..1) onto the photo region with v0=10/240, v1=230/240 (top-down) — the snap-FB sub-rect was registered N64-orientation, but the mesh wanted bottom-up orientation, so it rendered Y-inverted. 1P stage clear was unaffected on D3D11/Metal because `sc1pstageclear.c:2268-2300` walks top-down and its registration matches its heap. Fix part A (decomp): swap v0 ↔ v1 in the VS bridge call so `Interpreter::ImportTexture`'s `FbUvTransform` ends up with negative `scaleV` — equivalent to the original heap's bottom-up byte order. Fix part B (libultraship, OpenGL only): `GfxRenderingAPIOGL::CreateFramebuffer` was never registering the FB color attachment in the `textures[]` vector, so the GLSL shader's `texture_width/height` uniforms read 0 for any FB-passthrough draw → `0.5/texSize` divide-by-zero in `clamp()` and `filter3point()` — the 1P "all-black" symptom and the VS post-v-swap halt. Fix: register `clrbuf` in `textures` at create time and keep its dims in sync in `UpdateFramebufferParameters`. Fix part C (libultraship, OpenGL only): OpenGL's FBO sample direction is opposite of its render direction when `invertY=true` (storage row 0 holds game-top per the vertex Y negation, but `GL_TEXTURE_2D` sample reads rows in the opposite order), so `V=10/240` actually samples close to game-bottom. Added `GfxRenderingAPI::FbNeedsSampleVFlip(int fbId)` — default false; OGL returns the FB's `invertY`. `Interpreter::ImportTexture`'s FB-passthrough hook now applies a post-transform V-flip (`scaleV = -scaleV`, `offsetV = 1 - offsetV`) when the backend reports it's needed. D3D11 and Metal use the default false and are unchanged. Audit hook: any future N64-FB-mirror registration must inspect the *direction* of the original RDRAM-write loop, not just its rectangle — bottom-up loops register with `v0 > v1`. |
| 2026-05-08 | [audio_envmix_ramp_overflow](audio_envmix_ramp_overflow_2026-05-08.md) | **RESOLVED (issue #108)** — Hit SFX corrupted at extreme stage \|X\| (Sector Z left cockpit, right below-wing; same on any wide stage; character- and move-independent). Bus int32 `dry_left_acc` formed a 2 kHz sawtooth (16-sample period) on whichever channel was being driven near the int16 rail by the panning law. `port/audio/mixer.c::ramp_step` did `value += step` in plain int32; when `_getRate` returned the "saturate-immediately" sentinel `ratem=0x7FFF, ratel=0xFFFF` (= INT32_MAX rate, common on voice start with `em_segEnd==0` and `tgt>=cvol`) and the dominant channel's target sat near `INT32_MAX` (= em_volume × n_eqpower[≈0] = 29542 << 16 ≈ 1.94e9), the per-sample sum overflowed signed int32 and wrapped to a large negative. The existing reach check `value >= target` then read false (negative < positive target), the ramp didn't clamp, l_vol came out as -31898 instead of +29541, and the cycle repeated every ~16 samples. Fix accumulates through int64 and clamps to int32 range before the reach check — sentinel rate now collapses to the intended one-sample saturation. Verified: 178 huge L-jumps in the 30 s WAV (baseline) → 0 (fixed); L peak 32767 (rail) → 27720 (clean). Class: N64 RSP ucode "magic" rate/count values handled correctly by the original microcode but driven into int32 wrap territory when the op is reimplemented as scalar CPU arithmetic. Sibling: `audio_adpcm_codebook_stale_slots_2026-05-02` |
| 2026-05-04 | [raphnet_get_version_retry](raphnet_get_version_retry_2026-05-04.md) | **RESOLVED** — v0.7.4-beta native raphnet path failed to open the GC/N64-to-USB v3 adapter (vid=0x289b pid=0x0061) on Windows: defensive `SUSPEND_POLLING(0)` returned a valid 8-byte opcode-echo response but the very next `GET_VERSION (0x04)` read came back `n=0 err=Success` and Open() bailed. `RaphnetTransport::Exchange` early-exited on `if (got > 0)` and computed `payloadLen = got - 1`, returning 0 bytes when the device hadn't yet populated its response feature report. On Windows hidapi's `hid_get_feature_report` adds 1 to `bytes_returned` to account for the report-id byte (`hidapi/windows/hid.c::hid_get_feature_report`), so a "no payload" read surfaces as `got == 1` — which we treated as success rather than as the equivalent of Linux/macOS's `got == 0` "not ready, retry" sentinel. SUSPEND_POLLING happens to ack faster than the round-trip so its first GET sees real bytes; GET_VERSION needs a moment of firmware processing and its first GET sees only the report-id byte. raphnet's reference `gcn64tools/src/rntlib/raphnetadapter.c::rnt_exchange` polls `HidD_GetFeature` until `res_len > 0` with a ~1000 ms ceiling — we already had the loop, but only retried on the Linux-shaped sentinel. Fix changes the Exchange early-exit from `if (got > 0)` to `if (got > 1)` so a zero-payload read on either platform falls through to the retry path; deadline-trip warning now logs `last_got` so future repros distinguish 0 vs 1. Same path serves SUSPEND_POLLING(1), per-frame Poll, GetControllerType, and SetVibration so all benefit. Audit hook: any new `hid_get_feature_report` caller needs the same "`got == 1` means zero payload on Windows" awareness — treat `got - 1` as the meaningful byte count, not `got` itself |
| 2026-05-03 | [samus_charge_loop_sfx_pause_leak](samus_charge_loop_sfx_pause_leak_2026-05-03.md) | **RESOLVED (hotfix)** — Pausing while Samus charged her neutral-special left the plasma "whoosh" (`fp->p_loop_sfx`) audibly looping through the pause menu and on into the resumed game until her status next changed. On IDO/N64 the audio thread is gated each frame by `osRecvMesg(&sSYAudioSPTaskMesgQueue)`; while the pause menu owns the SP, that message stops landing, the FGM bytecode tick (`func_80026B90_27790` / `func_80027460_28060`) stops advancing, and the looping voice naturally winds down because the wavetable loop region stops getting re-armed by `case 1` ticks. The port's audio thread (`decomp/src/sys/audio.c:1139` PORT branch) has no SP and no handshake — it runs on VI tick alone, so the bytecode keeps cycling and the voice keeps playing. Polite `ftParamStopLoopSFX(fp)` route doesn't work here because `siz34->unk_0x28` (the EE0C ptr cached when the bytecode allocated the current note's voice) goes stale across the bytecode's natural case-2→0→free cycle; per-frame trace logged `on_0x3C=0` for every audio frame after pause — the EE0C we wrote `unk2A=2` to was already freed and the audible voice was on a different EE0C the cached ptr never pointed at. Fix: new `portAudioPurgeFGMs` in `n_env.c` walks `unk_alsound_0x3C` directly and calls `n_alSynStopVoice` + `n_alSynFreeVoice` on every voice (synthesizer-level kill, bypasses the envelope-mixer fade-to-zero path), then NULLs every siz34's `unk_0x28`. `ftParamStopAllFightersLoopSFX` clears each fighter's `p_loop_sfx` cache and delegates the kill to the new routine. Hooked into `ifCommonBattlePauseInitInterface` before the existing `func_80026594_27194` call. Hotfix only — proper root cause is "port audio thread doesn't deschedule like the SP-gated N64 thread"; real fixes ((a) gate `n_alAudioFrame` on game state, or (b) lock `siz34->unk_0x28` reads/writes) have wider blast radius and are deferred. Audit hook: anywhere on N64 voice shutdown was implicit-by-SP-starvation (cinematic transitions, scene swaps) is a candidate for the same hotfix shape on the port |
| 2026-05-03 | [opengl_fb_capture_y_flip_double_invert](opengl_fb_capture_y_flip_double_invert_2026-05-03.md) | **RESOLVED** — 1P stage-clear frozen-frame wallpaper rendered upside-down on OpenGL only (D3D11 / Metal correct). Original capture path (commit `a2697fb`, Metal-verified) speculatively row-flipped on OpenGL on the "glReadPixels uses bottom-left origin" assumption. That assumption is wrong for `mGameFb`: libultraship creates it with `opengl_invertY=true` (interpreter.cpp:5893), so the engine pre-negates vertex Y at draw time (interpreter.cpp:2761) and the OGL framebuffer's bottom row is already the N64 *top* row. The wrapper's compensating flip therefore inverted a buffer that was already in N64 orientation. D3D11 / Metal `ReadFramebufferToCPU` implementations walk textures in native top-left order with no analogous flag, so they were correct without any flip. Confirmed via a short-lived `SSB64_FB_FLIP=always\|never\|auto` env-var harness — only `never` produced an upright wallpaper. Fix: drop the OpenGL-only Y-flip; `gfx_read_fb_handler_custom` (interpreter.cpp:5036) for the GBI-driven readback path corroborates that all backends deliver N64 top-down rows for `invertY=true` framebuffers. Audit hook: a future caller of `ReadFramebufferToCPU` on `mGameFbMsaaResolved` (the only LUS FB built with `invertY=false`) on OpenGL is the next likely place this class of bug surfaces |
| 2026-05-03 | [oscontinit_locate_throw_ci_install_prefix](oscontinit_locate_throw_ci_install_prefix_2026-05-03.md) | **RESOLVED** — Windows v0.7.2 release crashed during boot on user machines with `class std::filesystem::filesystem_error` (`exists: The device is not ready.: "D:/a/BattleShip/BattleShip/BattleShip/gamecontrollerdb.txt"`) followed by `Exception: 0xe06d7363`. Two compounding bugs: (1) `scripts/package-windows.ps1` ran `cmake -DNON_PORTABLE=ON -DCMAKE_INSTALL_PREFIX=BattleShip` which CMake resolves against the configure cwd, baking the GitHub Actions runner workspace `D:/a/<repo>/...` into libultraship's `install_config.h` — a path meaningless on user machines. (2) `Ship::Context::LocateFileAcrossAppDirs` used the throwing `std::filesystem::exists(p)` overload, which rethrows `ERROR_NOT_READY` (drive present but no media) as `filesystem_error`. The throw unwound out of `osContInit`'s controller-init thread, no catch in scope, `terminate()` fired, spdlog's async pool tore down on the way out. Port already worked around the bogus app-bundle path for f3d.o2r / BattleShip.o2r via `ssb64::PortLocateFile` (issue #58); `gamecontrollerdb.txt` was the one remaining caller through the upstream throwing helper. Fix in libultraship: switch `LocateFileAcrossAppDirs` to the noexcept `exists(p, ec)` overload + wrap the `gamecontrollerdb.txt` load in `osContInit` in try/catch as defense-in-depth. Bump submodule pointer. LUS commit `d388467d` on `JRickey/libultraship#ssb64`. Audit hook: any LUS API probing the filesystem with the throwing `std::filesystem::exists(p)` is a latent Windows crash on non-ENOENT failures |
| 2026-05-03 | [g_vtx_unresolved_addr_guard](g_vtx_unresolved_addr_guard_2026-05-03.md) | **PARTIAL — defensive (issue #103)** — v0.7-beta SIGSEGV in `Fast::Interpreter::GfxSpVertex+0x84` with `fault_addr=0x248d2a` after classic-mode stage transitions (BTT → CSS, classic round → next round). Visual precursors (flickering green DK head, audio cut-out) confirm GBI-stream corruption upstream of the crash. `Interpreter::SegAddr` falls through every resolution path and returns the raw N64-shaped value as a host pointer; `gfx_vtx_handler_f3dex2` passes it to `GfxSpVertex`, which dereferences and SIGSEGVs at `fault_addr = (uintptr_t)w1`. Upstream LUS PR #1042 added the same guard for SETTIMG but never propagated it to the three G_VTX handlers. Defensive fix: filter `<= 0x0FFFFFFF` resolved addresses in `gfx_vtx_handler_f3dex2/f3dex/f3d`, `SPDLOG_ERROR` with `n_vertices`/`dest`/raw `w1` so future repros yield diagnostics instead of crash addresses. **Does not fix root cause** — the bad pointer is being seeded several frames upstream by suspected heap-reuse / scene-transition state leak (same family as `btt_result_screen_state_leak`, `feedback_overlay_bss_audit`). Root-cause hunt is open. LUS commit `9c186811` on `JRickey/libultraship#ssb64` |
| 2026-05-02 | [intro_run_impact_flash_unstub](intro_run_impact_flash_unstub_2026-05-02.md) | **RESOLVED (issue #73)** — Attract-loop's `nSCKindOpeningRun` (scene 38, "Characters running on purple background") was missing both the impact-flash mesh and the explosion boom at tic 190 (climax just before transitioning to OpeningCliff). Cause: `mvOpeningRunFuncRun` had a `#ifdef PORT` block (added in `66e108c` 2026-04-11) that stubbed both `mvOpeningRunMakeCrash()` and the paired `func_800269C0_275C0(nSYAudioFGMExplodeL)` to dodge a Fast3D draw-time deadlock when `MVOpeningRunCrash` (file 0x4B) was first rendered. The same commit shipped the *real* fix in `port/bridge/lbreloc_byteswap.cpp` (protected-range list preventing `portRelocFixupTextureAtRuntime` from re-BSWAP'ing the bitmap-array region after over-sized TLUT loads) plus a NULL-bitmap log-and-skip safety net in `lbcommon.c`. Those infrastructure fixes are sufficient — the game-side stub was belt-and-suspenders that suppressed a working effect. Audio-side `func_800269C0_275C0` stub in `n_env.c` was already removed by post-`016fb7e5` audio work; FGM `nSYAudioFGMExplodeL` is the same SFX bound to bombs/capsules/eggs in gameplay. Fix: delete the `#ifdef PORT` block, restore the original two-line decomp call. Verified visually (purple/yellow ray-burst mesh renders) by booting `SSB64_START_SCENE=38` and waiting through the natural 2250-tic pre-roll to tic 190 |
| 2026-05-02 | [intro_jungle_state_leak](intro_jungle_state_leak_2026-05-02.md) | **RESOLVED (issue #71)** — Opening attract sequence broke on second watch (loop-back via demo cycle or Title→ModeSelect→Title): scene chain stalled silently mid-intro. `mvopeningjungle.c` was the lone opening overlay missing an `*InitTotalTimeTics()` reset — every peer (cliff/sector/standoff/yamabuki/yoster/clash) zeros its counter from `FuncStart`, but Jungle relied implicitly on the N64 BSS-DMA-zero on overlay re-entry. On PC, statically-linked overlays leave `sMVOpeningJungleTotalTimeTics` at 320 (the previous iteration's exit value) so the `== 320` transition condition at FuncRun:370 had already-fired-and-passed, the counter rolled past on the next tic, and Jungle dead-ended. Fix matches the peer pattern verbatim — adds `mvOpeningJungleInitTotalTimeTics()` called from `FuncStart`. Same class as `btt_result_screen_state_leak`. Worth proposing upstream to `VetriTheRetri/ssb-decomp-re` as a decomp consistency fix. Audit hook: every `mvopening*.c` `*TotalTimeTics`-style counter must be reset on `FuncStart` entry; subagent audit verified no other instances |
| 2026-05-02 | [audio_adpcm_codebook_stale_slots](audio_adpcm_codebook_stale_slots_2026-05-02.md) | **RESOLVED** — Sector Z Arwing whoosh + strong-hit SFX (PunchL/KickL/BurnL/ShockL/SlashL/BatHit) caused garbled audio with discontinuities concentrated at every 16-sample ADPCM frame boundary (90% of jumps at `idx%16==15`). `aLoadADPCMImpl` did `memcpy(rspa.adpcm_table, book, count)` with `count = 128` (npredictors=4) into a 256-byte buffer; slots `[4..7]` retained the previous voice's book. Any wave whose ADPCM frames encoded `table_index ∈ {4,5,6,7}` then decoded with another sample's predictor coefficients → ~12000-amplitude jumps every affected frame. On N64 the equivalent unused tail was wave-specific deterministic ROM bytes (never read), so the issue was port-introduced when the microcode was reimplemented as CPU memcpy. Fix: `memset` the full 256-byte table before each `memcpy`, plus bounds-clamp count to prevent rspa overrun. Audit: any RSP microcode op reimplemented as CPU code that writes into shared `rspa.*` state needs to normalize the unused tail, not just copy `count` bytes |
| 2026-05-02 | [inishie_efgrounddatas_array_short](inishie_efgrounddatas_array_short_2026-05-02.md) | **RESOLVED** — Selecting Mushroom Kingdom in VS Mode SIGSEGVed at stage-load (`efGroundSetupRandomWeights+176`, `fault_addr=0x10000000a`). `dEFGroundDatas[]` in `src/ef/efground.c` has 8 entries (Castle…Yamabuki) but `nGRKindInishie = 8`, so `efGroundMakeAppearActor` reads `dEFGroundDatas[8]` past the end of the array. On LP64 the trailing bytes look like a non-NULL `effect_params`, the existing `!= NULL` guard passes, and `efGroundSetupRandomWeights` then dereferences a bogus pointer. Fix: append a NULL stub entry (same shape as Hyrule's `{0, NULL, 0x0, NULL}`) so the guard correctly skips the unlockable stage. Audit hook: any per-gkind table indexed up to `nGRKindBattleEnd` must have 9 slots, not 8. |
| 2026-05-01 | [sdl2_backend_destroy_uninit_pointers](sdl2_backend_destroy_uninit_pointers_2026-05-01.md) | **RESOLVED** — Every clean shutdown of the Linux AppImage segfaulted in `libSDL2-2.0.so.0+0x5468f` after `PortGameShutdown returned`, regardless of session length. `GfxWindowBackendSDL2` declared `mWnd` / `mCtx` / `mRenderer` without member initializers; `Init()` is path-dependent (OpenGL sets `mCtx`, Metal sets `mRenderer`, neither path touches the other) but `Destroy()` unconditionally fed every field — including the still-uninitialized garbage one — to SDL_DestroyRenderer / SDL_GL_DeleteContext. Compounding bug: `SDL_DestroyRenderer` ran *after* `SDL_DestroyWindow`, dereferencing freed window memory. Fix: zero-init the three pointer members + null-guard each Destroy call + reorder renderer-before-window. Diagnostic fingerprint: PC fixed at one libSDL2 offset, `lr=0` with one-frame backtrace, `fault_addr` near `fp` in heap region, reproducible on a 2-second session — independent of save data, scene state, or gameplay. LUS commit `053ec17` on `JRickey/libultraship#ssb64` |
| 2026-05-01 | [attract_freeze_vi_idle_present](attract_freeze_vi_idle_present_2026-05-01.md) | **RESOLVED (issue #9)** — Attract-demo freeze moments had the right game-side timing but held a stale host swapchain image on zero-submit VI frames, so portrait/banner freezes could appear one frame early and audio/camera cuts advanced incorrectly. Original hardware keeps VI scanning the current RDRAM framebuffer even when no new display list is submitted. Fix: add a Fast3D idle-present path that redraws the cached game framebuffer texture through the normal window/GUI present path on zero-submit VIs, without re-running display lists or touching scene state |
| 2026-05-01 | [zako_stock_sprite_unfixed](zako_stock_sprite_unfixed_2026-05-01.md) | **RESOLVED (issue #53 lag/visual)** — After the polygon-Pikachu LP64 crash fix unblocked stage 12, the renderer flooded ~280k `RelocPointerTable: invalid/stale token` errors per run at `lbcommon.c:2427` and the polygon stock icons rendered at the wrong screen position. `sc1PGameInitTeamStockDisplay` allocated 30 placeholder sobjs (`SC1PGAME_STAGE_MAX_TEAM_COUNT`) for the polygon kill counter, and the per-frame proc copied `*lbRelocGetFileData(Sprite*, sSC1PGameZakoStockFile, llFTStocksZakoSprite)` straight into each `sobj->sprite` without ever calling `portFixupSprite` on the source. The post-pass1-BSWAP32 sprite still had every `{s16,s16}` pair byte-swapped, so `sprite->nbitmaps` (offset 0x28) returned the original `ndisplist` value (36), and the renderer walked 36 garbage Bitmap entries every frame, each failing PORT_RESOLVE generation check synchronously — the actual lag mechanism. Yoshi/Kirby team stocks at the same call site work because their `stock_sprite` came in via the fighter-attribute load path which already fixes up; only the Zako one-off file loader was missing the hook. Fix calls `portFixupSprite` + `portFixupBitmapArray` + `portFixupSpriteBitmapData` once at file load (idempotent on pointer key, so the per-frame copies inherit the fix). Drive-by also added bzero-on-NULL-sprite in `gcAddSObjForGObj` so the freelist's prior occupant doesn't leak through `attr = SP_HIDDEN` placeholder paths. Audit hook: every `lbRelocGetFileData(Sprite*, ...)` call site needs a fixup chain before reaching the renderer |
| 2026-05-01 | [npikachu_particlebankid_lp64_overrun](npikachu_particlebankid_lp64_overrun_2026-05-01.md) | **RESOLVED (issue #53 crash)** — Polygon Pikachu spawn crashed with `fault_addr=0x400000004`-shape pointers when `ftMainSetStatus` read `*fp->data->p_file_submotion`. `dFTNPikachuData` declared `p_file_special2 = (void**)&gFTDataNPikachuParticleBankID` where the slot was `s32` (4 bytes). On N64 the cast was harmless because pointers were 4 bytes; on LP64 `*p_file_special2 = lbRelocGetStatusBufferFile(...)` writes 8 bytes, and the BSS layout placed `SubMotion` 4 bytes after `ParticleBankID` so the write straddled into SubMotion's low half. Same family as MBall/Kirby file_head and ALParam-overrun bugs. Fix widens `ParticleBankID` to `void *` (the slot was misnamed by decomp — never read as a particle id, only used as the special2-file pointer slot) and drops the `(void**)` cast. Lag follow-up resolved separately as `zako_stock_sprite_unfixed` (this same series) |
| 2026-05-01 | [itattackevent_size_double_fixup](itattackevent_size_double_fixup_2026-05-01.md) | **RESOLVED** — Bob-omb / Capsule / Mr. Saturn / Box / Marumine / Link-Bomb / Porygon / Venusaur explosions all had `attack_coll.size = 0` because `portFixupStructU16(&ev[i], 0x04, 1)` was rotating bytes at offset 0x04 *after* pass1 BSWAP32 had already placed the size correctly at struct offset 0x06. Leftover fixup from the 2026-04-20 sizeof=12→8 struct rewrite — the call should have been deleted, was instead retargeted from offset 0x08 to 0x04. Net effect: detection hitboxes (sized from `attr->size * 0.5F`) hit fine for 1% but the subsequent expanding-explosion events read size=0 → "tiny / misses." Fix: remove the 7 size-fixup calls; keep the 2 legitimate `fgm_id @ 0x20` fixups in `itporygon.c` / `itfushigibana.c`. Class: fixup-left-over-after-struct-rewrite. Verified via `reloc_extract.py` (sizes 350/250/150/0 in ROM at offset 0x04) + IDO probe of BE struct emitting `lhu 0x6($a0)` for `ev->size` |
| 2026-05-01 | [fog_alpha_source_mux](fog_alpha_source_mux_2026-05-01.md) | **RESOLVED (issue #31)** — Fighter colour-anim flashes (respawn-platform white, Mario fireball red self-tint, Pikachu hit red/yellow flicker, Pikachu F-Smash white) were invisible. LUS Fast3D's blender decoder only inspected the cycle-1 *colour* source (`other_mode_l >> 30`), never the alpha source — so `G_RM_FOG_PRIM_A` (alpha = `G_BL_A_FOG`, the alpha component of the fog colour register set by `gDPSetFogColor`) silently took the same code path as `G_RM_FOG_SHADE_A` (alpha = `G_BL_A_SHADE`, per-vertex). The shader's fog blend factor came from `vertex.color.a` (≈ 0xFF for opaque fighters) instead of `mRdp->fog_color.a`, so the game's intended `gDPSetFogColor(255,255,255,A)` ramp never tinted anything. End-to-end port-side instrumentation (COLANIM/FOGCALC/SETFOG/FOGDRAW) proved the bytecode interpreter, fog calc, and render-mode selection were all correct under PORT — bug isolated entirely to libultraship's blender bit decoding. Fix decodes cycle-1 alpha-source mux (bits 26..27) and feeds `mRdp->fog_color.a` when `G_BL_A_FOG` is selected; preserves shade-alpha path for OoT-style depth fog. Same family as `primdepth_unimplemented_2026-04-25` (whole RDP selectors collapsed to the one OoT/MM uses). LUS commit `e608ec09` on `JRickey/libultraship#ssb64` |
| 2026-05-01 | [boss_defeat_sfx_max_typepun](boss_defeat_sfx_max_typepun_2026-05-01.md) | **RESOLVED** — Defeating Master Hand crashed the audio thread inside the FGM bytecode interpreter (`func_80027460_28060`) reading a NULL/garbage `ucode` pointer. `D_8009EDD0_406D0` is **defined** in `n_env.c` as `ALWhatever8009EDD0` (with `u8 **fgm_table_data`, `u16 fgm_ucode_count`) but **declared** in `sc1pgame.c` as `extern alSoundEffect` (with `u16 sfx_max`). On N64 the two struct layouts coincidentally aligned at byte offset `0x28`, so the boss-defeat code's `D_8009EDD0_406D0.sfx_max = 0` was an intentional N64-only type-pun for "set fgm_ucode_count to 0 → block FGM playback during the cinematic." On LP64 the layouts diverge: ALPlayer grew 16→32 and three `**` fields each grew 4→8 in ALWhatever8009EDD0 (so fgm_ucode_count shifted to 0x40), while alSoundEffect's four `void*` widened (so sfx_max shifted to 0x38). At LP64 offset 0x38 in ALWhatever8009EDD0 lives the **low 16 bits of `fgm_table_data` — a `u8**` pointer**. The boss-defeat write zeroed those 16 bits, every subsequent `fgm_table_data[id]` read garbage, and the audio thread crashed on the next FGM-tick dereference. Diagnostic fingerprint: `0x100b251b8 → 0x100b20000` (low 16 bits cleared, high 48 unchanged — exactly a u16 write of 0). Fix: two `#ifdef PORT` accessors `portAudioSaveAndBlockFGMs / portAudioRestoreFGMs` operating on `fgm_ucode_count` directly; sc1pgame.c routes through them. Same family as `oscontpad_lus_sizeof_overrun_2026-04-24` |
| 2026-05-01 | [boss_event32_cache_invalidation](boss_event32_cache_invalidation_2026-05-01.md) | **RESOLVED** — Master Hand's okutsubushi (palm slam), okupunch (rocket punch), and drill animations rendered wrong (boss parked at z=-15000, never returned forward; drill upside-down). `port_aobj_event32_unhalfswap_stream`'s walker caches three sets of "already processed" verdicts keyed on heap pointer (`sUnhalfswappedVisited`, `sUnswappedHeads`, `sRejectedHeads`). `port_aobj_register_halfswapped_range` evicted only the first cache when a new figatree heap range was registered; the other two retained stale entries from a previous animation's load at the same heap addresses. Subsequent loads of file 2127 (`FTMasterHandAnim029`) at the reused address short-circuited the walker, leaving fresh halfswap-corrupted bytes un-fixed. Parser hit "UNHANDLED opcode=64 — ending anim" and TransN.translate stayed (0,0,0) for the rest of the attack. Fix adds the same eviction loop for the two missing caches in lockstep with the existing one. Initial wrong-hypothesis detour: a ftPhysicsGetRootMotionJoint(TransN→XRotN→YRotN) resolver was written and discarded once per-frame telemetry showed TransN existed and was the right joint — the issue was bytes, not the joint slot. Class: caches keyed on heap pointer must evict on heap reuse — same shape as `project_fixup_idempotency_heap_reuse`. Systemic audit: 7 pointer-keyed caches across `port/`, all now correctly evicted; no other latent instances |
| 2026-04-30 | [audio_fgm_unk_flat_parse](audio_fgm_unk_flat_parse_2026-04-30.md) | **RESOLVED** — Star-KO chime (FGM 0xc) sounded as a sustained high-pitched ring instead of a clean rising chime; other SFX similarly missed envelope decay. `fgm_unk` was being parsed as if it had the same `[count][offset[count]][payloads]` layout as `fgm_tbl`/`fgm_ucd`, with `unk44` set to the resulting pointer table. The actual consumer `unk_alsound_0x24[i]` indexes with 16-byte stride (size of `ALWhatever8009EE0C_3`: 4 u8 + 3 f32), so reads landed inside the pointer table for `i ≥ 1` and drifted into adjacent heap. The f32 fields also weren't BE→LE bswapped. Result: every envelope shape decoded as `f4=f8=fC≈0` (denormal/zero) → no decay → constant amplitude for the bytecode's full duration; the chime's 150-frame sustain made it audible. Fix: parse `fgm_unk` as `[BE u32 count][16-byte struct[count]]` (no offset table — verified by diagnostic dump), allocate flat `count*16` bytes, copy u8 fields raw and `bswap32` the f32 fields. An earlier draft assumed an interleaved offset-table layout and SIGBUSed at audio thread setup on the first iteration (offset reads of struct[0]'s u8 fields decoded to a wild address). Class: don't reuse a parser for a "looks similar" file format without verifying the actual bytes |
| 2026-04-30 | [fttexturepart_container_byteswap](fttexturepart_container_byteswap_2026-04-30.md) | **RESOLVED (issue #30)** — Pikachu F-Smash changed the wrong face material slot. Pass1 BSWAP32 corrupts the 6-byte u8-only `FTTexturePartContainer` and pass1-byte 0 reads as original byte 3, etc. The first attempt (commit `435c944`, reverted in `1f2b694`) wrote 8 corrected bytes back into the file image — fine for Pikachu (zero pad at offsets 6-7) but crashed CSS for fighters whose adjacent file bytes were a u32 reloc token (`SIGSEGV fault_addr=0x0` in `ftMainSetStatus` at win-pose `status=0x10003`, because `PORT_RESOLVE` of the now-corrupted token returned NULL). A per-byte attempt that only touched bytes 0-5 also crashed at `0xffff000100000037` for fighters where bytes 4-5 themselves were inside an aliased token. Final fix: `portFixupFTTexturePartContainer` returns a static-storage **corrected COPY** of the 6 bytes (plus 2 zero pad) instead of writing through the file image. Caller dereferences the returned pointer for `textureparts[i]` reads. The file image is never modified, so no adjacent token data can be corrupted. Class: any "fix u8 fields after pass1" must avoid in-place writes when the underlying memory may be aliased with adjacent reloc-token fields |
| 2026-04-30 | [btt_result_screen_state_leak](btt_result_screen_state_leak_2026-04-30.md) | **RESOLVED (issue #27)** — 10/10 BTT result screen skipped the "TIMER × N" tally step and froze with looping audio. `sc1PStageClearInitVars` explicitly zeroes most stage-clear globals but leaves `CommonAdvanceTic`, `BonusShowNextTic`, `BonusAdvanceTic`, `BonusNum` untouched — N64 overlay re-DMA zeroed them every entry but PC has overlays statically linked. After Fox's stage clear left them at `130/170/190`, the BTT result screen's tic counter coincidentally matched `CommonAdvanceTic` at `tic=130`, firing the cleanup-and-transition block on the same frame as `MakeTimerTextSObjs` — ejecting the freshly-built timer GObj, running `MakeBonusTable` prematurely, and computing `BonusShowNextTic` to a value already in the past so auto-advance never fired again. Fix: reset the four globals under `#ifdef PORT` at the start of `InitVars`. Class: any per-scene `InitVars` that relies on overlay-load to zero BSS leaks state on PC |
| 2026-04-29 | [master_hand_mobjsub_lp64_stride](master_hand_mobjsub_lp64_stride_2026-04-29.md) | **RESOLVED (issue #15)** — 1P Master Hand fight crashed with SIGSEGV on stage load (`sc1PGameBossSetupBackgroundDObjs + 512`, fault addr `0x840002c400000000` — token in pointer high half). Boss-wallpaper code raw-casts file bytes to `MObjSub***` and walks both pointer levels with native arithmetic; on LP64 each `++` strides 8 bytes over 4-byte file slots and each `*` reads in half of the next slot. The earlier `db533f0` MObjSub fixup is correct but unreachable — the loop crashes before resolving a real `MObjSub*`. Fix walks both levels as `u32*` + `PORT_RESOLVE` under `#ifdef PORT`, mirroring the `sc1pbonusstage` `anim_joints` pattern; non-PORT path preserved verbatim. Same family as the existing **File pointer-array LP64 stride** class. Boot test hook (`SSB64_START_SCENE=17 SSB64_SPGAME_STAGE=13`) added so the boss is reproducible without playing through the run |
| 2026-04-29 | [btt_arrow_loadtlut_dma_alignment](btt_arrow_loadtlut_dma_alignment_2026-04-29.md) | **RESOLVED (issue #4)** — BTT/BTP arrow rendered with pink/magenta outline + per-character inner color on every stage except Link's. Two compounding bugs: (1) `chain_fixup_settimg` rounded `tex_bytes` UP but the runtime fixup `portRelocFixupTextureAtRuntime` rounded `num_bytes` DOWN to a 4-byte boundary, leaving word 1 of count=3 partial-palette TLUT bytes (containing pal[2..3]) in pass1-BSWAP state — `0xC17B` magenta-pink at pal[2] became the visible "pink outline." (2) `GfxDpLoadTlut`'s exact-byteCount memcpy didn't match N64 RDP DMA which transfers in 8-byte chunks — count=3's 6-byte memcpy left palette_staging[0][6..7] (= pal[3]) holding stale bytes from the prior LOADTLUT (typically MarioModel character TLUT, `0x003D` deep blue), producing the per-character "blue inside the chevron." Fingerprint: Link works because his data uses count=4 (8 bytes, already DMA-aligned, no quirk-reliance); every other character's data lays out a 4-entry palette and counts on N64's 8-byte DMA alignment to load the 4th entry "for free" with their count=3 cmd. Fix: round `byteCount` UP to 8 bytes in `GfxDpLoadTlut`, round `num_bytes` UP to 4 bytes in `portRelocFixupTextureAtRuntime`, and have `chain_fixup_settimg` populate `sTexFixupExtent`+`sTexFixupWords` so the runtime path falls through correctly |
| 2026-04-29 | [audio_output_filter_overshoot_clip](audio_output_filter_overshoot_clip_2026-04-29.md) | **SUPERSEDED 2026-05-08** (root cause was the int32 envmix ramp wrap fixed in `audio_envmix_ramp_overflow_2026-05-08`; biquad headroom factor reverted) — DK win-pose growl (FGM 0x146) garbled when the output low-pass filter was active (default-on); `SSB64_AUDIO_OUTPUT_FILTER=0` cleaned it; Yoshi voice unaffected. Cause: `applyOutputFilter`'s 14.5 kHz Butterworth biquad sits at 0.91·Nyquist where the bilinear transform warps the analog 4.3% step-response overshoot up to ~14.6% in the discrete filter. For input near int16 max (DK's deep growl peaks the rails), overshoot drives output past int16 → `clampS16FromDouble` flat-tops at ±32767 → square-wave harmonics → "garbled noise." Yoshi peaks lower, overshoot stays under int16. Fix: pre-attenuate b0/b1/b2 by 0.85 so peak step response ≈ 0.974 of input — no flat-tops on int16-max input. Cost ~1.4 dB on filtered path, anti-imaging effectiveness unchanged. Diagnostic that nailed it: forcing `sFilteredBuf[i] = input[i]` (passthrough through the filter buffer with no biquad math) → DK clean → math itself was the issue, not routing/upstream saturation. Class: discrete IIR designed via bilinear at near-Nyquist fc needs hand-checked step response, not just magnitude |
| 2026-04-29 | [audio_int32_acc_save_load_bridge](audio_int32_acc_save_load_bridge_2026-04-29.md) | **SUPERSEDED 2026-05-08** (int32 wet/dry bus accumulators reverted with `d30a967`; mirror code obsolete because s16 DMEM is now the only state again) — Reverb wet bus invisible to the FX delay-line save/load round-trip. Voices' `aEnvMixerImpl` correctly accumulated wet samples into `rspa.wet_left_acc[]` (int32), but `aSaveBufferImpl` / `aLoadBufferImpl` only memcpy through the s16 `BUF_U8`/`BUF_S16` mirror — so when `n_alFxPull` ran `_n_saveBuffer(N_AL_AUX_L_OUT → r->input)` it copied an empty s16 region to the delay-line DRAM, processed zeros, and `aMix(N_AL_AUX_L_OUT → N_AL_MAIN_L_OUT)` added zero back to dry. Symptom: BGM and SFX played but reverb was missing — flagged audibly by jtl3d / MarioReincarnate. Fix: bridge int32 acc ↔ s16 mirror inside aSaveBufferImpl (clamp16 acc → s16 before memcpy) and aLoadBufferImpl (s16 → acc after memcpy). Verified: wet bus peaks 0 → ~1k, dry bus picks up the wet contribution, audible reverb returns |
| 2026-04-29 | [css_winpose_script_null_addend](css_winpose_script_null_addend_2026-04-29.md) | **RESOLVED (issue #8)** — CSS win-pose body animation played but per-character VFX/SFX never fired (Yoshi voice, DK growl, Link swoosh, Samus sword unsheathe). `ftMainSetStatus` resolves the motion script as `motion_desc->offset + *p_file_submotion`. For opening/win/results entries, `motion_desc->offset` is already a fully-resolved `D_ovl1_*` script pointer cast through `(intptr_t)`; the addend `*p_file_submotion` is NULL on most fighters and the BE/N64 NULL-deref returned 0, so `pointer + 0 = pointer` worked. Commit `f255803` (2026-04-06) added a defensive NULL-guard on both the deref AND the addend; the addend guard turned `pointer + 0` into NULL → script never parsed. Fix drops the addend guard, keeps the deref guard. Three call sites in the same function. Documents the offset-as-pointer convention so the next defensive pass doesn't re-introduce it |
| 2026-04-29 | [fighter_slope_contour_lp64_alias](fighter_slope_contour_lp64_alias_2026-04-29.md) | **RESOLVED (issue #5)** — Fighter foot contouring on slopes read a cached transform vector through an N64-only `FTParts*`-as-`DObj*` alias in `func_ovl2_800EBC0C`. On LP64, widened `DObj` pointers moved `DObj::rotate.vec.f` away from the intended `FTParts::unk_dobjtrans_0x10[2]` row, so Kirby/Samus/DK foot IK used the wrong orientation. Fix keeps the decomp alias for non-port builds and reads the intended `FTParts` matrix row explicitly under `PORT` |
| 2026-04-29 | [css_gate_palette_overlap_fixup](css_gate_palette_overlap_fixup_2026-04-29.md) | **RESOLVED (issue #3)** — Character Select gate panels rendered with state-dependent garbled colors because adjacent small TLUT blocks were each requested as 512-byte runtime palette fixups. `portRelocFixupTextureAtRuntime` was idempotent only by starting address, so panel 2/3/4 loads could byte-swap overlapping palette words a second time after panel 1 had already fixed them. Fix tracks runtime texture/TLUT fixups per u32 word (`sTexFixupWords`) while preserving exact-base pass2/chain skips |
| 2026-04-29 | [css_reentry_status_selected_stale](css_reentry_status_selected_stale_2026-04-29.md) | **RESOLVED (issue #7)** — Re-entering 1P or Training-Mode CSS rendered the previously-chosen character facing the camera in default idle instead of replaying their selected/win pose. `mnPlayers1PGameInitPlayer` / `mnPlayers1PTrainingInitPlayer` set `is_fighter_selected = TRUE` on re-entry but don't reset the sibling `is_status_selected` flag — `*FighterProcUpdate` then sees a fresh fighter at rotation 0 with the stale `is_status_selected = TRUE` and skips the `scSubsysFighterSetStatus(... Win ...)` call. VS InitPlayer explicitly resets the flag (which is why VS re-entry works); the 1P/training InitPlayer functions don't. Fix mirrors the VS reset under `#ifdef PORT` |
| 2026-04-29 | [cpu_emblem_color_fixup](cpu_emblem_color_fixup_2026-04-29.md) | **RESOLVED (issue #6)** — CPU fighters' damage-display series icons rendered cyan instead of their per-stage CPU emblem color. The original game treats `MPGroundData::unused` as a 5th emblem-color slot — `mnplayersvs.c:4424` sets CPU `player.color = GMCOMMON_PLAYERS_MAX` (= 4) and `ifCommonPlayerDamageInitInterface` reads `emblem_colors[4]` as a deliberate one-past-the-array index. The port-side `mpCollisionFixGroundDataLayout` un-byteswaps fog_color through emblem_colors[3] but stops one u32 short of `unused`, so the bytes the C code reads as `emblem_colors[4]` stay BSWAP32-permuted — most stages' near-gray turns into ≈ `(00, FF, FF)` ≈ cyan. Fix: extend the fixup range to cover `unused`. HMN unaffected because `players[i].color = i` (HMN) indexes inside the declared 4-entry array |
| 2026-04-29 | [ia4_upload_unclamped_to_tile](ia4_upload_unclamped_to_tile_2026-04-29.md) | **RESOLVED (issue #2)** — VS Record digit sprites (4×7 IA4) rendered as ~1-pixel-wide vertical strokes instead of full glyphs ("squished" digits). `Interpreter::ImportTextureIA4` uploaded the unclamped TMEM-line-derived natural width (16 pixels) while `GfxSpTri1` normalised UVs by the SetTileSize-clamped tex_width (4 pixels), so the four output pixels ended up sampling GPU columns ≈ 0/4/8/12 of an upload whose useful digit data only occupied columns 0–3. Mirror of the IA8/I4 ClampUploadWidthToTile fix from title_border_right_edge_slice_2026-04-14. I8 and IA16 still latent for the same reason if a sprite triggers it |
| 2026-04-29 | [training_mode_sprite_init_pre_fixup](training_mode_sprite_init_pre_fixup_2026-04-29.md) | **RESOLVED (issue #1)** — Training Mode digit / combo / item / CP / speed / view / pause-menu option sprites rendered with wrong tint, missing alpha, or didn't render at all (only the first option of each pause-menu row drew correctly). `sc1PTrainingModeInitStatDisplayCharacterSprites` and `sc1PTrainingModeInitMenuOptionSpriteAttrs` write `red`/`green`/`blue`/`attr` directly into `display_option_sprites[0..38]` and `menu_option_sprites[0..30]` BEFORE any `lbCommonMakeSObjForGObj` runs — i.e. before `portFixupSprite` rotates the post-pass1 byte order back to native LE. Writes land at the wrong offsets, the deferred fixup then shuffles them out (e.g. `attr → zdepth`), and most slots never even reach the fixup chain because the per-frame digit / option update path is `sobj->sprite = *sprites[modulo]` struct copy. Fix adds `sc1PTrainingModeFixupSprite` (full Sprite + BitmapArray + texel-data fixup) and calls it inside both Init loops before the field stores; idempotent so the later Make-time fixup is a no-op |
| 2026-04-28 | [sector_arwing_interp_desc_byteswap](sector_arwing_interp_desc_byteswap_2026-04-28.md) | **RESOLVED** — Sector Z's patrol Arwing flew through the Great Fox because `SYInterpDesc` word 0 was decoded using the fighter-figatree halfswapped byte layout even for normal stage/movie resources. Non-figatree descriptors only had pass1 `BSWAP32`, so `kind` / `points_num` came from the wrong bytes and the Arwing evaluated the wrong spline. Fix: normalize descriptor word 0 with `portFixupStructU16` for non-figatree data, while keeping figatree-only un-halfswap for full-width f32 fields/data blocks |
| 2026-04-28 | [bumper_wrong_colors](bumper_wrong_colors_2026-04-28.md) | **RESOLVED** — NBumper (item kind 16) rendered solid blue after landing and pink on collision because its landed/wait material was swapped in manually without the port-side `portFixupMObjSub` pass used by normal object setup. Fix: run `portFixupMObjSub` before `gcAddMObjForDObj` in `itNBumperAttachedInitVars`; user verification confirmed normal colors after landing and hit |
| 2026-04-26 | [intro_explosion_overlay_layering](intro_explosion_overlay_layering_2026-04-26.md) | **RESOLVED** — mvOpeningRoom desk→arena transition explosion (tics 1040–1080) had two visual bugs: (1) opaque white center where the red/white silhouette should be, and (2) effect rendered behind all scene geometry, visible *through* the desk-room window in the wallpaper. Both rooted in libultraship's Fast3D not emulating the N64 color-image-redirect-to-Z-buffer idiom. The Overlay's white-PRIM mesh, intended to write a Z mask, instead painted directly onto the framebuffer (white blob). And the transition camera's `dl_link_priority=95` made it draw earliest (= bottom of stack) — on N64 this didn't matter because the redirect handled layering via depth manipulation, not draw order. Fix in two PORT-gated hunks in `mvopeningroom.c`: early-return from the Overlay display callback (the mesh contributes nothing visible without redirect emulation), and lower the transition camera's `dl_link_priority` 95 → 30 so it draws after fighter=40 / main=80 / wallpaper=90. Supersedes the earlier partial libultraship fixes documented in `docs/intro_explosion_alpha_cutout_handoff_2026-04-25.md`; remove both PORT branches if approach #4 (real redirect emulation) ever ships |
| 2026-04-25 | [primdepth_unimplemented](primdepth_unimplemented_2026-04-25.md) | **RESOLVED** — `gDPSetPrimDepth` was a no-op stub in libultraship's interpreter (latent since the original Emill Fast3D engine; upstream Kenix3 still carries the same TODO). Game sprites that set `gDPSetDepthSource(G_ZS_PRIM)` ended up at clip-space z=0 (front plane) instead of their intended depth — port-wide layering fail. Most visible: 2D portrait card drew in front of the 3D fighter model in description scenes. Found while debugging the mvOpeningRoom transition explosion (wallpaper at `gDPSetPrimDepth(36863, 1)` was occluding the explosion tris). Fix in 3 hunks: store `prim_depth_z`/`prim_depth_dz` in the RDP struct, parse in `gfx_set_prim_depth_handler_rdp`, override per-vertex Z in `GfxSpTri1` when `other_mode_l & G_ZS_PRIM`. Linear `primDepth/65535` mapping into clip space — sufficient for sprite layering. The explosion itself is still hidden — separate alpha-cutout-coverage gap, see `docs/intro_explosion_alpha_cutout_handoff_2026-04-25.md` |
| 2026-04-25 | [spline_interp_block_halfswap](spline_interp_block_halfswap_2026-04-25.md) | **RESOLVED** — Samus's dodge / morph-ball roll played the animation but didn't translate her position. `SetTranslateInterp` events in fighter figatrees point to a `SYInterpDesc` plus three full-width data tables (`Vec3f points`, `f32 keyframes`, `f32 quartics`). The figatree halfswap pass swaps u16 halves of every non-token u32 in the file — correct for AObjEvent16 streams, wrong for f32/Vec3f data. Fix un-halfswaps each figatree data block on first access, decodes figatree descriptor `kind`/`points_num` through the PORT LE layout, and un-halfswaps full-width descriptor f32s. Same family as `aobjevent32_halfswap_2026-04-18`; follow-up for non-figatree descriptors: `sector_arwing_interp_desc_byteswap_2026-04-28.md` |
| 2026-04-25 | [whispy_canopy_metal_fallback_texture](whispy_canopy_metal_fallback_texture_2026-04-25.md) | **RESOLVED** — Pupupu Whispy canopy stripe / matanim feedback artifact on macOS Metal, present since VS mode first booted. CC modes that declare TEXEL1 without a matching `LoadBlock` for tile 1 left `mCurrentTextureIds[1]` at its zero-init default; in libultraship's Metal backend that index belongs to the screen drawable (`mTextures[0]`), so the encoder bound the live color buffer at sampler slot 1 and the fragment shader sampled the in-progress screen — `mix(curr, screenfb, frac)` produced the stripe. OpenGL's GLD driver substitutes a zero texture for unbound samplers (the `GLD_TEXTURE_INDEX_2D unloadable` warning called out in `port/port.cpp`); Metal-cpp does no such substitution. Fix lives in libultraship: allocate a 1×1 transparent-black RGBA texture in `gfx_metal.cpp::Init()`, point `mCurrentTextureIds[]` at it by default, and redirect the bind in `DrawTriangles` whenever the resolved slot would alias `mTextures[0]` or has null tex/sampler. Supersedes the `SSB64_CURR_EQ_NEXT=1` / `port/port.cpp` workaround |
| 2026-04-24 | [audio_mixer_macro_clobber](audio_mixer_macro_clobber_2026-04-24.md) | **RESOLVED — BGM audible.** Final unblock after the bank-swap and ALParam-variant fixes. `include/PR/abi.h` auto-included `port/audio/mixer.h` at its tail, but `n_abi.h` (defines `n_aLoadBuffer`/`n_aADPCMdec`/etc.) was included LATER via `n_synthInternals.h`. Result: mixer.h's `#undef + redefine` of the n_a* family ran while those macros didn't yet exist (no-op `#undef`s, redefines applied), then n_abi.h silently overrode mixer.h's redefinitions back to the original Acmd-write macros. Pull chain wrote Acmd words that nothing consumed → silent BGM. The standard `aXxx` family was unaffected (defined before mixer.h ran, never redefined). Fix: removed the auto-include from abi.h; n_env.c already includes mixer.h after the n_audio headers. Verified: `first non-zero sample detected` fires; BGM 33 plays |
| 2026-04-24 | [audio_alparam_variant_overrun](audio_alparam_variant_overrun_2026-04-24.md) | **RESOLVED** — Immediately after the bank-swap fix, BGM 33 crashed in `n_alEnvmixerPull+100` with `fault_addr=0x7f0d`. Root cause: `__n_allocParam()` returns 40-byte ALParam slots, but `ALStartParamAlt` grew to 48 bytes on LP64 (its `wave *` widened 4→8 plus a 4-byte alignment pad pushed `unk1C/unk1D` to offsets 40/41). `n_alSynStartVoiceParams_Alt`'s writes overran the freelist's adjacent param's `next` pointer; the byte pattern `unk1C=0x05, unk1D=0x7F` produced exactly the corrupt em_ctrlList=0x0000000000007F05 we saw. Fix pads ALParam to 48 bytes under PORT and adds `_Static_assert(sizeof(variant) <= sizeof(ALParam))` for ALStartParamAlt/ALStartParam/ALFreeParam |
| 2026-04-24 | [audio_bridge_bank_swap](audio_bridge_bank_swap_2026-04-24.md) | **Fix shipped; awaiting in-game audible verification.** `audio_bridge.cpp` loaded `sounds2_ctl` into `sSYAudioSequenceBank2` (music CSP slot) and `sounds1_ctl` into `sSYAudioSequenceBank1` (SFX direct-index slot) — the original game's `SYAudioSettings` does the opposite (`bank2_start = sounds1_ctl`, `bank1_start = sounds2_ctl`). The decomp's `// load sfx bank` comment above the `sSYAudioSequenceBank2` assignment misled the bridge author; downstream `n_alCSPSetBank(player, sSYAudioSequenceBank2)` makes that slot the music bank. Result: music CSP got the 322-sound SFX bank with degenerate keymaps, every NoteOn missed in `__n_lookupSoundQuick` → silent BGM. Fix swaps the two parser blocks |
| 2026-04-24 | [audio_cseqplayer_altbank_lp64](audio_cseqplayer_altbank_lp64_2026-04-24.md) | **Fix shipped; audio still silent for unrelated reason.** `N_ALCSPlayer` / `N_ALSeqPlayer` store two extra bank slots as `s32 unknown0/unknown1` right after `ALBank *bank`, and MIDI CC22 picks between them via `*(&seqp->bank + idx)`. On LP64 the pointer is 8 bytes but the s32s are 4, so the pointer-array idiom reads garbage after the first slot. Also `(s32)(intptr_t)` truncates bank pointers on write. Widened both to `ALBank *` under PORT and dropped the truncating casts. BGM 33 doesn't exercise alt-banks, so the change is a latent-bug fix — synth is still producing zeros because of a separate music-bank keymap lookup issue (handoff `docs/audio_buildout_2026-04-24.md`) |
| 2026-04-24 | [training_mode_sprite_array_stride](training_mode_sprite_array_stride_2026-04-24.md) | **RESOLVED** — Training Mode entry LP64 pointer-array stride sweep: three successive crashes in the same scene-start path (`fault_addr=0x1fe00140024` then `0x20200000219` etc., all adjacent-token-as-u64 fingerprints). Affected: `SC1PTrainingModeSprites::sprite` (8B→16B struct stride), `SC1PTrainingModeMenu::display_option_sprites` (`Sprite**` → 4B→8B slot stride), `menu_option_sprites` (same). All pinned to `u32`/`u32*` under PORT with `PORT_RESOLVE` at readers and `portFixupStructU16` on the Vec2h pos word. Same class as bonus-stage `anim_joints` and mball `file_head` |
| 2026-04-24 | [training_mode_wallpaper_token_truncation](training_mode_wallpaper_token_truncation_2026-04-24.md) | **RESOLVED** — Training Mode entry SIGSEGV `fault_addr=0x34` inside `lbCommonMakeSObjForGObj` via `grWallpaperMakeStatic`. `MPGroundData::wallpaper` is `Sprite*` on N64 but `u32` token on the port. `sc1PTrainingModeLoadWallpaper` wrote the raw host pointer — the only post-load writer of this field — truncated to 32 bits on LP64, and `grWallpaperMakeStatic`'s `PORT_RESOLVE` returned NULL for the bogus "token." Fix: wrap the write in `PORT_REGISTER(sprite)` so it round-trips through the pointer table |
| 2026-04-24 | [oscontpad_lus_sizeof_overrun](oscontpad_lus_sizeof_overrun_2026-04-24.md) | **RESOLVED** — Menu-only input double/triple-fire (pause toggles, CSS puck grab-then-drop, CPU level jumps 2–3). `OSContPad` has two layouts in the same build: decomp 6-byte (game C) vs libultraship 0x24-byte (C++ stub). `libultraship/.../os.cpp osContGetReadData` used `sizeof(OSContPad)` (LUS) and memset'd 144 bytes into a 24-byte caller buffer, zeroing the leading fields of the adjacent `sSYControllerDescs[]` every Read. `unk00` (prior-frame button state for edge detection) went to 0 each frame → held button re-fired `button_tap` every tick. Fix marshals through a local LUS-layout stack buffer and writes only the 4 decomp fields into the caller's pointer. `button_hold`-driven gameplay inputs were unaffected because `unk00` gets restamped with `button` a few lines after the clobber |
| 2026-04-24 | [itattributes_rom_layout_rewrite](itattributes_rom_layout_rewrite_2026-04-24.md) | **RESOLVED** — No thrown item (Pokeball, Capsule, Egg, Barrel, Link's Bomb, GShell) ever connected with a fighter mid-flight. Root cause: port `ITAttributes` sizeof was `0x50`, true ROM stride is `0x48`; artificial `u32 : 11/16/14` pads in the LE branch shifted every field from `0x1E` onward by 2 bytes and pushed `attack_count` onto pad bits (read 0 → hit loop never ran). `type` and `hitstatus` also declared in a nonexistent final u32 — they live in the same u32 as `priority` / `kb_base` / `can_*` at `0x3C`. Rewrote struct against ground truth from disassembling `itManagerMakeItem` at `0x8016E174`. Supersedes `itattributes_type_field_offset` and `item_map_coll_bottom_sign` workarounds |
| 2026-04-22 | [loadtile_fixup_last_row_right_edge](loadtile_fixup_last_row_right_edge_2026-04-22.md) | **RESOLVED** — Yoster 5 thin horizontal semi-opaque stripes. `GfxDpLoadTile` byte-order fixup used `start_offset_bytes + orig_size_bytes` (tile stride × tile_h) as its upper bound, but the decoder reads at full-image stride. For any LoadTile where `tile_width < full_image_width`, the last row's rightmost `(full_w - tile_w)` pixels stayed in pass1-BSWAP32 (LE) state. Byte-reversed RGBA puts the background's `F7` pink-channel byte into the alpha slot, drawing an opaque strip at the bottom of every sub-tile load. Fix: `tile_height * full_image_line_size_bytes` as the fixup extent |
| 2026-04-22 | [mpgrounddata_layer_mask_u8_byteswap](mpgrounddata_layer_mask_u8_byteswap_2026-04-22.md) | **RESOLVED** — Sector Z Great Fox hull missing (and related partial-render reports on other stages). `MPGroundData.layer_mask` is a `u8` at struct offset 68; pass-1 blanket BSWAP32 pushes the real value to offset 71, so the native `u8` read picks up the zero padding byte. `grDisplayMakeGeometryLayer` then always takes the pri_proc_display branch (`gcDrawDObjTreeForGObj`) instead of sec_proc_display (`gcDrawDObjTreeDLLinksForGObj`), which walks DObjDLLink chains as raw Gfx*. Nine sequential `{list_id, dl_token}` pairs decode as G_NOOP runs with token payloads (fingerprint: `bytes=00000000 00000131 00000004 00000000` at d=4 after a d=3 G_DL). Fix adds `portFixupStructU32` helper to un-swap the layer_mask u32 word at stage init. Also resurfaces the class: any u8 in a loaded struct controlling behaviour needs the same audit |
| 2026-04-22 | [fighter_appearr_body_frozen](fighter_appearr_body_frozen_2026-04-22.md) | **RESOLVED** — Mario / Link Appear intros rendered bodies frozen in Wait stance while entry effects (pipe, beam, capsule) animated correctly. Regression from 3161c13's walker hardening: the new "raw form also parses → reject" rule triggered on one-step End false positives (halfswap-corrupted u32s whose bits [31:25] happen to equal 0), causing the walker to reject halfswap-corrupted streams as "ambiguous native." Parser read the raw bytes, hit End immediately, terminated every body joint's animation on the first parse call. Fix: replace binary ambiguity rule with length comparison — raw must walk ≥ 2 commands to count as genuinely native, otherwise un-halfswap wins |
| 2026-04-20 | [particle_banks_silent_empty](particle_banks_silent_empty_2026-04-20.md) | **RESOLVED** — Every in-game particle effect (hit sparks, fire-flower fire, explosion flame, Samus fire, Mario coins, fireball sparkles, stage hazards, title/attract particles) silently failed to render. The 2026-04-08 DMA-segfault fix papered over the crash by registering a dummy bank without calling `lbParticleSetupBankID`, so every `lbParticleMakeChildScriptID` bounds check (`script_id >= sLBParticleScriptBanksNum[id]`) failed against `scripts_num == 0` and returned NULL silently. Fix: new `port/bridge/particle_bank_bridge.cpp` loads all 9 banks from `particles/*` in ssb64.o2r, byte-swaps the headers (bytecode is left alone — `lbParticleReadFloatBigEnd`/`ReadUShort` are already LE-safe), calls `lbParticleSetupBankID`. Dedup via pristine-path cache + scripts_lo-keyed working-copy eviction. Supersedes `particle_bank_dma_2026-04-08.md` |
| 2026-04-20 | [color_image_to_zbuffer_draws](color_image_to_zbuffer_draws_2026-04-20.md) | **RESOLVED (TextureRectangle only)** — Off-screen fighter magnifier bubble rendered as an opaque black square with the fighter icon inside. SSB64 uses an N64 trick that redirects the color image to the Z buffer to build an alpha-masked stencil before the visible XLU draw. Fast3D doesn't emulate address redirection — all draws go to the primary FB — so the mask draw landed as a visible opaque rectangle. Fix adds the `color_image == z_buf_address` skip (already present in `GfxDpFillRectangle`) to `GfxDpTextureRectangle`. `GfxSpTri1` still needs the same guard if `mvOpeningRoom` transitions ever regress |
| 2026-04-20 | [sprite_decode_stride_mismatch](sprite_decode_stride_mismatch_2026-04-20.md) | **RESOLVED** — Tutorial banner / textbox / HereText arrow / fighter-select puck / 1P character portraits / 1P map wallpaper all rendered with diagonal shear. `ImportTextureRgba16` / `ImportTextureRgba32` / `ImportTextureCi8` reassigned `fullImageLineSizeBytes` from the *clamped* `width` when SetTileSize made `tex_width < width_img`, so the decode read every subsequent row from the wrong offset. Generalizes the 2026-04-14 IA8/I4 ClampUploadWidthToTile fix; CI8 also had a stacked decode-before-clamp bug that required restructuring |
| 2026-04-20 | [sprite_32bpp_tmem_swizzle](sprite_32bpp_tmem_swizzle_2026-04-20.md) | **RESOLVED** — RGBA32 sprites (SMASH logo, fighter portraits, any 32bpp) rendered with scanline shear because the TMEM-line-swizzle fix from 2026-04-10 hardcoded the 4b/8b/16b swap granularity (XOR-4) and skipped 32bpp entirely. Real fix: for 32bpp, swap 8-byte halves of 16-byte groups (XOR-16 at DRAM level) because `G_IM_SIZ_32b_LOAD_BLOCK = G_IM_SIZ_32b` puts each texel in two TMEM words via bank split |
| 2026-04-20 | [wpattributes_bitfield_padding](wpattributes_bitfield_padding_2026-04-20.md) | **RESOLVED** — Mario 64% damage / Fox 0 damage / Samus pass-through all one bug: port's `WPAttributes` bitfield positions didn't match IDO BE's physical layout. IDO packs `angle:10` into the u16 pad at 0x26, and `knockback_weight` belongs to Word 1 not Word 2. Verified by disassembling IDO-compiled reader. See [docs/debug_ido_bitfield_layout.md](../debug_ido_bitfield_layout.md) for the audit method |
| 2026-04-20 | [itattributes_type_field_offset](itattributes_type_field_offset_2026-04-20.md) | **SUPERSEDED** by 2026-04-24 itattributes_rom_layout_rewrite: the surgical `type`-override workaround was removed when the full struct got rewritten; `attr->type` now reads natively |
| 2026-04-20 | [item_map_coll_bottom_sign](item_map_coll_bottom_sign_2026-04-20.md) | **SUPERSEDED** by 2026-04-24 itattributes_rom_layout_rewrite: the "ROM stores as positive magnitude" observation was wrong — the old struct was reading `map_coll_width`'s positive bytes from the wrong offset; the real `map_coll_bottom` at `0x2E` is already signed-negative in ROM. Workaround's negation removed |
| 2026-04-20 | [item_arrow_gobj_implicit_int](item_arrow_gobj_implicit_int_2026-04-20.md) | Dropped-item despawn segfault: `ifCommonItemArrowMakeInterface` called without prototype → implicit-int rule truncates the 64-bit `GObj*` return to 32 bits on LP64 |
| 2026-04-20 | [mnplayers1pgame_timegobj_stale](mnplayers1pgame_timegobj_stale_2026-04-20.md) | 1P character-select crash on re-entry from VS: `sMNPlayers1PGameTimeGObj` static slot was missing from `mnPlayers1PGameInitVars`'s reset list, so its stale value survived the per-scene heap reset and `gcEjectGObj` then dereferenced freed memory |
| 2026-04-19 | [rumble_event_bitfield_init](rumble_event_bitfield_init_2026-04-19.md) | Battle-start hang: positional initializers on endian-conditional bitfield stored opcode as param; gmRumbleUpdateEventExecute spins on phantom End |
| 2026-04-18 | [aobjevent32_halfswap](aobjevent32_halfswap_2026-04-18.md) | Fighter figatree u16-halfswap corrupts AObjEvent32 command bitfields; lazy per-stream un-halfswap walker at EVENT32 reader entry |
| 2026-04-14 | [title_border_right_edge_slice](title_border_right_edge_slice_2026-04-14.md) | ce89700's zero-fill of sprite trailing columns dimmed the right edge of the title-screen border sprite; fix replicates edge pixel instead |
| 2026-04-13 | [samus_charge_shot_hit_detection](samus_charge_shot_hit_detection_2026-04-13.md) | **SUPERSEDED** by the 04-20 `WPAttributes` bitfield fix: `attack_count` was being decoded from the wrong bits (read 0 instead of the true 1), so the hit loop never iterated. The bypass-path theory was a red herring |
| 2026-04-11 | [mpvertex_byte_swap](mpvertex_byte_swap_2026-04-11.md) | MPVertexData / MPVertexIDs byte-swap deferral broke Jungle floor collision |
| 2026-04-11 | [controller_motorevt_lp64](controller_motorevt_lp64_2026-04-11.md) | Unk80045268 ContMotorEvt struct pun faults on LP64 |
| 2026-04-11 | [fighter_tile_masks_wrap](fighter_tile_masks_wrap_2026-04-11.md) | Missing SetTile masks/maskt clamp → black squares on fighter body parts |
| 2026-04-11 | [loadtlut_cross_boundary](loadtlut_cross_boundary_2026-04-11.md) | LOADTLUT spillover across 256-byte palette boundary dropped entries |
| 2026-04-11 | [osmesg_union](osmesg_union_2026-04-11.md) | OSMesg `void*` vs union type split produced uninitialized upper bytes |
| 2026-04-11 | [bzero_recursion_arm64](bzero_recursion_arm64_2026-04-11.md) | Stubbed bzero on macOS/arm64 recursed infinitely via memset lowering |
| 2026-04-10 | [sprite_texel_tmem_swizzle](sprite_texel_tmem_swizzle_2026-04-10.md) | Sprite byteswap deferral + N64 RDP TMEM line swizzle left sprites sheared |
| 2026-04-08 | [segment_0e_gdl](segment_0e_gdl_2026-04-08.md) | Segment 0x0E G_DL resolution exception branched into the graphics heap |
| 2026-04-08 | [particle_bank_dma](particle_bank_dma_2026-04-08.md) | **SUPERSEDED** by 2026-04-20 particle_banks_silent_empty: avoided the segfault but left every bank empty, silently breaking all particle effects |
| 2026-04-08 | [ground_geometry_collision](ground_geometry_collision_2026-04-08.md) | PORT_RESOLVE + all-u16 collision struct byte-swap fixes |
| 2026-04-07 | [dl_normalization_guard](dl_normalization_guard_2026-04-07.md) | Heuristic guard mis-detected packed DLs starting with w1=0 sync commands |
| 2026-04-06 | [display_list_widening](display_list_widening_2026-04-06.md) | Packed 8-byte resource DLs needed widening to 16-byte native format |
